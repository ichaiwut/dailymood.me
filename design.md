# DailyMood.me — Design

## Design Direction
- **Concept:** "DailyMood Web App" — warm cream canvas, Vercel/Linear-style desktop layout, vibrant purple/mint/peach palette
- **Reference:** Claude Design handoff (60 screens, 1280px desktop + 440px mobile artboards)
- **Approach:** Desktop-first responsive; topbar 64px full-bleed, content container max-width 1180px centered; mobile fallback with bottom nav
- **Layout pattern:** Topbar full-bleed + content centered (~1180px) — เหมือน Vercel/Linear
- **AI-slop avoidance:** No generic gradients or glassmorphism; color comes from mood palette, not decoration

## Typography (next/font)
- **UI sans (Latin)**: `Urbanist` (Google, weight 400-800) — all headings, body, buttons
- **UI sans (Thai)**: `Noto Sans Thai` (Google, weight 400-700) — Thai fallback
- **Style:** Bold (800) for headings, 700 for buttons/labels, 500-600 for body

## Color Palette (hex, light)
| Token | Value | Use |
|---|---|---|
| `--bg` | `#FBF6EE` | warm cream canvas |
| `--bg-grad` | `#FBF6EE` | flat warm cream (no gradient) |
| `--surface` | `#FFFFFF` | card backgrounds |
| `--surface-2` | `#F4EEE6` | warm tint tile |
| `--hairline` | `rgba(26,19,32,0.08)` | borders, dividers |
| `--hairline-2` | `rgba(26,19,32,0.12)` | stronger border |
| `--ink` | `#1A1320` | dark purple-black (primary text) |
| `--ink-2` | `#4A3F55` | secondary text |
| `--ink-3` | `#8C8497` | tertiary gray |
| `--primary` / `--purple` | `#A673F1` | primary brand (active nav, labels, avatar) |
| `--accent` / `--peach` | `#FCA45B` | CTA buttons, FAB, accent |
| `--mint` | `#85ECCB` | calm/positive mood |
| `--yellow` | `#FDCB56` | neutral mood |
| `--blue` | `#9ACDE2` | sad mood |
| `--lavender` | `#D4BEE4` | anxious mood |

## Mood Colors (7 moods)
| Mood | Emoji | Color | Score |
|---|---|---|---|
| Happy | 😄 | `#FCA45B` (peach) | 5 |
| Calm | 🙂 | `#85ECCB` (mint) | 4 |
| Neutral | 😐 | `#FDCB56` (yellow) | 3 |
| Sad | 😔 | `#9ACDE2` (blue) | 2 |
| Angry | 😠 | `#FEAD8D` (light peach) | 1 |
| Anxious | 😟 | `#D4BEE4` (lavender) | 2 |
| Tired | 😴 | `#A673F1` (purple) | 2 |

## Layout System
- **Frame:** 1280px (full page width including topbar)
- **Container:** `.w-container` max-width 1180px, margin 0 auto, padding 0 32px
- **Desktop topbar:** 64px height, full-bleed, backdrop-blur, horizontal nav (วันนี้/ปฏิทิน/สถิติ/AI) + "+ บันทึก" CTA + theme toggle (🌙/☀️ 36px square) + language toggle (🌐) + avatar
- **Mobile:** BottomNav (floating pill, peach FAB center), topbar hidden
- **Grid layouts:** `.grid-2col` (2.2fr 1fr), `.grid-sidebar` (320px 1fr), `.grid-stats-4` (4-col), `.home-grid` (1.7fr 1fr)
- **Auth:** `.auth-split` (1.1fr 1fr) — "Paper Desk" reading desk: articles-first paperclipped clippings left + paper sign-in slip right; mobile swaps to `.auth-mobile` (`MobileLoginFeed`) — articles feed + sticky CTA
- **Admin:** `.admin-layout` (240px sidebar 1fr) — dark sidebar (#1A1320)

## Visual Signatures
- **Rounded cards** — `.card` class, `border-radius: 14px`, white bg with 1px border `rgba(26,19,32,0.08)`
- **Brand mark** — `BrandMark` (`src/components/brand-mark.tsx`): the gradient smiley mark from `assets/logo.png`, inlined as a base64 data-URI (`src/lib/brand-logo.ts`) so it's identical + canvas-safe in both the header and the social share card. Paired with a themed "DailyMood" wordmark (text, not baked into the mark) so it reads on light + dark. Replaced the old hand-drawn `DMLogo` SVG (rounded-square gradient face) in both topbars.
- **Desktop topbar** — frosted glass (backdrop-blur + 85% opacity), `BrandMark` + "DailyMood" wordmark
- **Mobile bottom nav** — white pill (`border-radius: 38px`) with purple active state, peach FAB elevated center
- **Streak card** — right sidebar, large number (44px) + 14 streak bars + 🔥 emoji
- **AI weekly summary** — dark card (#1A1320→#2A1F33), golden sparkle icon (#FFC899), white text
- **Smart Log modal** — centered 720px dialog, backdrop blur, 22px radius, heavy shadow
- **Mood picker (hero)** — inline buttons in gradient hero card (lavender→purple), `minWidth: 72px`, `whiteSpace: nowrap`
- **Tag chips** — pill-shaped with white bg + 1px border or mood-colored bg for detected mood
- **Eyebrow labels** — `.w-eyebrow` class, 11px, weight 800, uppercase, letter-spacing .06em, `--ink-3` color
- **Stat cards** — left colored border (4px solid), large value (36px/800), eyebrow label above
- **Icon buttons** — 44×44px with 14px radius, `#F8F6FB` bg
- **Labels** — uppercase 12px `font-weight: 800`, purple color, 0.4px letter-spacing

## Pages / Screens

| Page | Description | Status |
|------|-------------|--------|
| Home | **Desktop:** 2-col grid (1.7fr main + 1fr sidebar). Main: greeting hero (lavender→purple gradient) with inline mood picker, AI composer card, day-axis timeline, 3-col entry grid. Sidebar: AI weekly summary (dark card), streak card (large number + 14 bars), mini calendar. **Mobile:** single column | Done (v2 redesign) |
| Login | **"Paper Desk · Sign-in slip"** split-screen. **Left (brand):** articles-first "read before you sign up" strategy rendered as paperclipped white `.pa-sheet` clippings (`PAClip` + framed cover [photo else `ArticleArt`] + tone category + title/excerpt/date, `pa-card-lift`), 📎 eyebrow + `PAMark` headline, "see all articles" paper folder footer — sits on the cream desk (`--bg`). **Right (form):** `LoginForm` as a purple folder-tab `.pa-sheet` ("✦ เข้าสู่ระบบ") with a `PAClip`, DM-logo brand header, paper inputs (`--w-surface-2`), chunky `.pa-btn` peach primary + paper-outline Google, danger-tint error box, email pill. **Mobile** (`MobileLoginFeed`): same paper clippings feed + sticky bottom CTA (paper Google + ink Email buttons). Sheet text `--w-ink*` (theme-adaptive, dark-safe), loose desk text theme `--ink*`. Email-first step machine + guest-token handoff + analytics unchanged | Done (v3 Paper Desk) |
| Smart Log | **Centered 720px modal dialog** (not full-screen). Backdrop blur rgba(26,19,32,.55). Header: sparkle icon + "บันทึกด้วย AI" + close X. Content: textarea with **mood-adaptive placeholder** (changes when mood is selected — Free: static prompts per mood, Premium: Gemini-personalized prompt), mic/image buttons, AI suggestion area, action buttons | Done (v2 redesign) |
| Calendar | **2-col grid** (2.2fr calendar + 1fr stats sidebar). Main: segmented toggle, AI summary, patterns, 7-col calendar grid, Ask AI bar. Sidebar: avg mood/streak/logged stat cards (colored left border) + mood legend | Done (v2 redesign) |
| Timeline | Integrated into Calendar tab via segmented toggle (Calendar/Timeline). Reverse-chronological feed grouped by day (TODAY/YESTERDAY/WEEKDAY), mood type filter chips, entry cards (48px mood swatch + title + time + 1-line note + tag emojis). Data: `/api/calendar/timeline`. `/history` redirects to `/calendar` | Done |
| Stats | Functional period toggle (Week/Month/Year, #F4F2F7 pill bg). Average mood line chart (purple #A673F1 gradient fill, adapts to period length) with **AI annotation pins** (Premium: glowing purple circles with pulse animation on anomaly/best/worst points, hover/tap shows dark tooltip with AI explanation + tag refs; Free: 1 blurred ghost pin + "✦ ปลดล็อก AI Annotations — PRO" upgrade CTA) + **Timeline Markers** (vertical dashed lines at holiday/personal event positions — red `#F43F5E` dashed for holidays, blue `#3B82F6` dashed for personal events, 40% opacity, emoji circle above chart at `PY-6`, rendered behind the line path). Delta badge (green ↑ / red ↓ vs previous period). Mood mix donut (center label adapts to period). Highest Mood Day card (full-width mood-colored bg + emoji). Real activity impact from tag-mood correlation — diverging bars (purple=positive, peach=negative), tag emoji heuristic, min 5 entries/tag, cap 6 rows. Free: rows 4-6 blurred with gradient overlay + "See all with Premium" pill. Year toggle disabled for free (dimmed + inline upsell toast). Insights link card (pastel gradient #FAF7FE→#FDE8DA). Bottom nav: Stats tab replaces Insights tab (bar chart icon) | Done |
| AI Insights | Hero gradient summary card (#A673F1→#C89BF5→#FCA45B) with Read Full (inline expand) + Share (Web Share API / clipboard fallback with "Copied!" toast). Pattern cards: tag badges (PATTERN orange #FCA45B, CORRELATION purple #A673F1, ALERT red #F26B6B) + optional sparkline SVG (60×24, purple line). Suggestion card (#FAFFF8 bg, #DEF1D5 border): green "TRY THIS" badge, thumbs up/down + "Add to routine" feedback pills (persisted in D1). Streak card (#FAF7FE bg, fire emoji). Free: hero preview (headline + first sentence only) + locked overlay. Premium: full feed. Cached weekly in D1 (delta-3 invalidation). Loading skeleton, empty state, too-few-entries state, error state. Back to Stats link in header | Done |
| Entry Detail | Mood hero card (full-width, mood-colored bg, giant faded 200px emoji), note section, AI summary gradient card (#F4EBFE→#FDE8DA) with Gemini-generated Thai summary (`**bold**` → `<strong>`; teaser fallback when no summary), tags pills, edit/compare buttons | Done |
| Edit Entry | **Desktop 2-col** (grid-2col 2.2fr 1fr). **Left:** page header (eyebrow "กำลังแก้ไข · บันทึกที่ N" + h1 "แก้ไขบันทึกของ{weekday} · {date}"), horizontal mood picker row (7 moods, selected=mood-colored bg, min-width 72px), 3-field date/time/period row (date input + time input + period dropdown เช้า/บ่าย/เย็น auto-derived from time), note textarea (500 char limit) with toolbar row (Bold B toggle=`**markdown**`, Voice mic, Image photo picker, ✦ AI re-analyze gradient button), error banner, tags section (# pills + inline add + suggested tags with dashed yellow border), image section (replace/delete overlay). **Right sidebar (sticky top:32px):** live preview card (mood-colored bg, mood icon 72px, label, weekday·time, period, tags joined by ·), AI insight card (sparkle icon + summary with **bold**→`<b>` + auto-reanalyze note), keyboard shortcuts card (⌘S save / Esc cancel / ⌘↵ AI — real `keydown` handlers), danger zone card (red 1.5px border #FCA5A5, warning text, × delete button → confirm modal). **Mobile:** sidebar hidden (`edit-sidebar` `!important`), date/time stacks vertically (1fr), toolbar wraps AI re-analyze to its own row, mood picker container has `overflow:hidden` to clip horizontal scroll. Danger zone duplicated below form via `edit-mobile-only` class (hidden on desktop). Bottom bar (Cancel + Save) rendered via `createPortal(document.body)` to escape main element's `z-index:0` stacking context, positioned `bottom:104px` to sit above BottomNav | Done (v2 redesign + mobile refinement) |
| Settings | Custom mood manager (Premium), mood pack picker (grid cards, 2-col, preview 4 icons per pack, selected=primary border + checkmark, PRO badge on premium packs, lock icon + "Upgrade →" for locked packs, instant save on tap with rollback on error) | Done (replaced by Profile → Settings) |
| Profile Overview | "You" tab. Hero card (purple→peach diagonal gradient, white text, 28px radius, avatar 76px initials circle with accent color + edit pencil overlay). Hero stats row (streak/entries/avg in translucent white panel). Mood signature card (eyebrow label, headline, horizontal stacked bar, top 3 caption). Achievements horizontal scroll (earned-only preview as Paper Desk **sticker discs** — solid mood-colour disc + white ring + drop shadow, matching `/profile/achievements`). Settings list (6 rows, color-tinted 42px icon tiles, chevron). Footer (help/sign out outline buttons, version). Bottom nav "You" tab → `/profile` | Done |
| Edit Profile | Modal top bar (back + "Edit profile" + Save purple text). Avatar 120px circle + camera FAB (📷 peach circle, triggers file input on click) + upload spinner overlay during upload + "Remove photo" button when custom avatar exists + premium teaser text for free users. Client-side optimize via `optimizeImage()` (WebP, 1600px max), 2MB server limit, R2 storage. Fields: Display name (gray pill input), Email (read-only + verified badge), Bio (textarea ≤160 chars). Delete account outline button at bottom | Done |
| Profile Settings | Grouped sections on tinted bg. Reminders (check-in toggle, time, days). Appearance (theme 3-option segmented: Light/Dark/Auto; mood palette 3-option: Neon/Tempered/Mono with swatch previews). Language (EN/TH radio). Privacy (toggles). Custom moods (Premium). Data (export, clear). About (help, feedback, terms) | Done |
| Achievements | **"Paper Desk · Sticker Album"** reskin. Wrapped in `.pa-wrap` on the themed body background (no cream desk — same as entry/article detail, so dark mode works). **Left rail** (`.ach-rail`, sticky desktop): paper back-pill → `pa-chip` "🏆 สมุดสะสม" eyebrow → `PAMark` headline "ความสำเร็จ ของคุณ" → gentle subtitle → **Progress folder** (`pa-tab.purple` + `.pa-sheet` + paper-toned ring [`--w-tint` track, peach progress, % + "X/Y unlocked" centre]; no washi — the folder tab is the paper signifier, tab+washi together clashed) → `pa-filter` pills (All/Earned/In progress/Locked + counts, ink active). **Sticker grid** (`.ach-grid`, 3-col → 2-col mobile → 1-col <380px): each badge is a slightly-rotated `.pa-sheet` "sticker card" (`.pa-card-lift` straighten on hover) with a 66px **sticker disc** (white ring + emoji). Three states read through paper materials: **earned** = vivid colour disc + colour-matched washi tape + tilted **rubber-stamp date** ("✓ 5 มิ.ย."); **in progress** = pale-tint disc + real `PAClip` + paper progress meter ("current / target" + colour-filled bar); **locked** = flat faded grayscale disc on a dashed "empty slot" sheet (title + desc still shown — teaches what unlocks it, no hiding). **Every badge is a `<button>` → opens `AchievementDetailSheet`** (Paper Desk content inside the generic `BottomSheet`): sticker hero + halo, title, desc, per-state body (earned = rubber-stamp date + celebratory line + Share button; in-progress = progress meter + "อีก X ก็ปลดล็อก"; locked = lock chip + gentle how-to-unlock). Earned → **`AchievementShareModal`** (light/dark, scaled preview, Share/Save/Copy) renders **`AchievementShareCard`** (1200×630: accent halo + giant sticker disc + emoji, "ปลดล็อกความสำเร็จ" eyebrow, title, unlock date, watermark) captured via `html-to-image` — same constraints as the mood share card (hardcoded HEX, no remote images). All text ≥14px | Done (v3 Paper Desk + share) |
| Subscription | `/profile/subscription`. Hero card (deep purple gradient #7B4FD3→#A673F1→#C89BF5, 28px radius, glow shadow). Status pill (✨ ACTIVE / ⏳ ENDING SOON). Plan title "DailyMood Pro" + renewal date. 2-col stat panel (NEXT CHARGE + MEMBER FOR) on translucent white. Billing section: NavRow rows (💳 Payment method, 📋 Billing history, 💡 Switch to yearly). All actions → Stripe Customer Portal. Cancel: outline gray button → BottomSheet (😢 emoji, confirm/keep buttons). Canceling state: peach notice band with resubscribe CTA. Free users: centered upgrade card with ✦ icon + "View Pro plans" link to `/pricing` | Done |
| Admin Dashboard | `/admin`. Sidebar layout (240px fixed, `var(--ink)` bg, peach active border). Gradient logo + "ADMIN" badge. SVG icon nav (7 items, incl. บทความ). Content: `var(--bg)` bg, max-width 1200px. TH-only. 4 KPI stat cards with delta (users, premium, Stripe revenue MTD, AI calls) + a "บทความ" engagement sub-section (folder-tab cards: ยอดอ่านบทความ, คนให้ความรู้สึก). DAU bar chart (30d, SVG). Recent users list. Auth: `ADMIN_EMAILS` env var | Done |
| Admin Users | `/admin/users`. Search + select filter. DataTable with avatar circles (peach→purple gradient), name, email, plan badge (AdminBadge premium/free), entry count, date, delete. Page number pagination | Done |
| Admin Entries | `/admin/entries`. DataTable with AdminBadge for AI source (info/neutral variants), mood emoji+label, user link. userId filter with clear button | Done |
| Admin AI Usage | `/admin/ai`. 4 stat cards (tokens, cost THB, NLP calls, Vision calls) + 2 cache stat cards. AdminBarChart (SVG, purple NLP + peach Vision stacked). Top users leaderboard. Token/cost tracking via `ai_usage.tokens_in/tokens_out/estimated_cost_thb` columns | Done |
| Admin Feedback | `/admin/feedback`. Status filter tabs (ทั้งหมด/รอดู/ตอบแล้ว/เก็บถาวร). Feedback items: type emoji, star rating, status badge (AdminBadge), archive action. Two-panel: user feedback + AI suggestion aggregation. DB: `feedbacks.type/rating/status` columns | Done |
| Admin Mood Packs | `/admin/packs`. 3-col grid cards. Pack card: 7-icon preview row (28px circles), label + tier badge (AdminBadge premium/free), edit/upload/delete buttons. Create form. SVG upload to R2 | Done |
| 404 Not Found | Standalone (no TopBar/BottomNav). Back arrow + "Error 404" header. Blue circle (200px) kawaii character (dot eyes + smile) with peach "?" floating badges. Faded "404" giant text behind (purple 8% opacity). Title + subtitle + body copy. Black pill "Back to home" button (home icon) + ghost "Report broken link" button (links to profile feedback). `fade-in` + `pop` animations | Done |
| 403 Forbidden | `/forbidden`. Standalone. Back arrow + "Error 403" header. Orange circle (200px) kawaii character (closed eyes + blush) with purple lock badge (44px circle, bottom center) + purple sparkle decorations. Faded "403" giant text. Purple uppercase "PRIVATE ENTRY" label. Title + body copy. Purple pill "Sign in to different account" button (lock icon) + ghost "Back to my journal" button | Done |
| Privacy | `/privacy`. Back arrow + "Privacy" title header. Hero card (purple gradient #7B4FD3→#A673F1→#C89BF5, 28px radius, shield 🛡️ floating icon, "YOUR DATA" label, bilingual title, 3 badge pills: ENCRYPTED / NO TRACKERS / NO ADS). "TL;DR" purple label + 4 summary rows in card (icon in colored square + title + body). 7 numbered sections (purple number circles + bold title + body paragraph with **bold** rendering). `fade-in` animation | Done |
| Terms | `/terms`. Back arrow + "Terms" title header. Hero card (peach gradient #E6952E→#FCA45B→#FDCB56, 28px radius, handshake 🤝 floating icon, "THE DEAL" label, bilingual title, effective date text). "THE SHORT VERSION" peach label + 4 summary rows (✅💛💎❤️ icons). 7 numbered sections (peach number circles). Same structure as Privacy | Done |

## Components

| Component | Purpose |
|---|---|
| `BottomSheet` | Generic reusable bottom sheet: scrim (`rgba(10,10,10,0.32)`), panel slides up from bottom (28px top corners), drag handle, body scroll lock, Escape dismiss, `sheet-open`/`sheet-close` CSS animation with deferred unmount |
| `EmptyToday` | Today-feed empty state (no entries today): centered `.pa-sheet` with lavender washi tape, a haloed mood `PASticker` (gentle `pa-float`), `emptyTitle` + `emptyBody`, and a purple `.pa-btn` ("write freely") that scrolls to + focuses the inline AI composer. Theme-aware (`--w-ink*`); soft, non-demanding copy per tone rule |
| `DaySheet` | Calendar day sheet content: date header with prev/next arrows, fetches `/api/log?date=`, renders `EntryMiniCard` list, empty state with "+ Log mood" CTA, loading skeleton |
| `EntryMiniCard` | Compact entry card for sheets: mood color bg (20% alpha) with ghost icon, mood label + time, 2-line note clamp, tag pills, Edit icon + "Open full entry" black pill CTA |
| `AiSummaryCard` | Calendar AI summary card: gradient bg (#FAF7FE→#FDE8DA), ✨ header, Gemini 2-3 sentence summary with **bold** → `<strong>`, 3 highlight chips (Best day/Hardest day/Top trigger), "Tell me more →" CTA. Free tier: 1st sentence + blurred chips + "Unlock with Premium" |
| `PatternsFeed` | AI pattern cards below calendar grid: icon + explanation + "View →". Locked state for free users |
| `AskAiBar` | NL search bar: dashed lavender border, rotating placeholder queries, expands on tap, inline answer with date chips |
| `TopBar` / `TopBarClient` | Purple avatar circle (initials) + day label + greeting with name + search/bell icon buttons |
| `BottomNav` | Floating white pill nav: Home, Calendar, FAB (+), Stats, Profile. Purple active, peach FAB |
| `HomeShell` | Home page: streak strip, AI composer, mood picker row, recent entries |
| `SmartLogModal` | Full modal: mood grid, note textarea (purple border), voice/camera, AI detection with gradient section, tag pills |
| `MoodIcon` | SVG mood icon from R2 pack, falls back to emoji |
| `MoodPicker` | 7-mood grid (legacy, replaced by inline row in HomeShell) |
| `LoginForm` | Email-first Linear-style: email → password / register / google_only / verify_sent |
| `VoiceButton` | Browser STT (Web Speech API), locale-aware (TH/EN) |
| `StatsShell` | Stats page: functional period toggle (Week/Month/Year with Year=premium gate), mood line chart card (SVG, adapts to period length, delta badge), mood mix donut + highest mood day 2-col grid, real activity impact panel (tag-mood correlation, diverging bars, premium blur on rows 4-6), Insights link card. Accepts `tier` prop |
| `InsightsShell` | AI Insights page: hero gradient summary card (Read Full inline expand + Share via Web Share/clipboard), pattern cards (tag badges + optional sparkline), suggestion card (thumbs up/down + "Add to routine" feedback pills → POST `/api/insights/feedback`), streak card. Free: preview + locked. Premium: full feed. Cached weekly. Accepts `tier` prop |
| `TimelineFeed` | Timeline view inside Calendar tab: mood type filter chips, day-grouped entry cards (mood swatch, title from aiSummary/note, time, tag emojis), tap → entry detail. Replaces `HistoryShell` |
| `HistoryShell` | ~~History/Timeline page~~ (deprecated — redirects to Calendar tab) |
| `EntryDetail` | Entry detail page: mood hero card with giant emoji + intensity bar, note section, AI summary gradient card, tag pills, edit/compare buttons |
| `ProfileShell` | Profile overview: hero gradient card (accent-aware gradient, avatar initials, stats row), mood signature card (stacked bar + headline), achievements horizontal scroll, settings list (icon tiles), sign out confirmation sheet. Accepts `tier` prop |
| `ProfileEditShell` | Edit profile form: avatar + accent color picker (6 colors), display name + email (read-only) + bio fields, delete account button. Fetches/patches `/api/profile` |
| `ProfileSettingsShell` | Settings: reminders section (toggle + time + days), appearance (theme + palette segmented pickers), language radio (EN/TH with locale switch), privacy toggles, custom moods (Premium), data (export + clear), about links. Accepts `isPremium` prop |
| `AchievementsShell` | Achievements "sticker album" (Paper Desk): `.pa-wrap` root, sticky left rail (back-pill + `pa-chip` eyebrow + `PAMark` headline + Progress folder with paper-toned SVG ring + `pa-filter` pills), `.ach-grid` of rotated `.pa-sheet` sticker cards. Sub-components: `BadgeStickerCard` (a `<button>` — earned/in-progress/locked: washi tape + rubber-stamp date / `PAClip` + progress meter / dashed faded slot) and `BadgeSticker` (66px disc + emoji). Tapping a card opens `AchievementDetailSheet`; the sheet's Share button opens `AchievementShareModal`. Fetches `/api/profile/achievements`. Data/API unchanged |
| `AchievementDetailSheet` | Paper Desk achievement detail rendered inside the generic `BottomSheet` (reuses its portal/scrim/Esc/close). Sticker hero + halo, title, desc, per-state body: earned = rubber-stamp date + line + `pa-btn` Share; in-progress = progress meter + "อีก X ก็ปลดล็อก"; locked = lock chip + gentle how-to-unlock. `onShare` lifts state to the shell |
| `AchievementShareCard` | `forwardRef` 1200×630 share card for a single badge: `palette(theme, badge.color)` gradient, accent halo + giant sticker disc + emoji, "ปลดล็อกความสำเร็จ" eyebrow, title, unlock date, badge description (how it was earned, e.g. "บันทึกอารมณ์ทุกวัน 7 วันติดต่อกัน"), shared `Watermark`. Hardcoded HEX, no remote images (emoji glyph), light/dark. Reuses `palette`/`mix`/`Watermark`/`THAI_FONT` exported from `share-card.tsx` |
| `AchievementShareModal` | Single-template share modal for achievements (light/dark toggle, scaled live preview, Share/Save/Copy). Capture/share/copy/download logic via the shared `useCardShare` hook. Analytics `trackShareAchievement(badgeId)`. zIndex above `BottomSheet` (10000) |
| `useCardShare` (hook) | Shared share-card capture logic extracted from the Stats modal pattern: `html-to-image` double-pass `toBlob` + Web Share API → clipboard → download fallbacks, busy + toast state. Params `{ cardRef, width, height, fileName, shareText, shareUrl, isTh, onShared }`. Consumed by `AchievementShareModal` (the Stats `ShareCardModal` keeps its inline copy to avoid regression) |
| `SubscriptionShell` | Subscription management: hero gradient card (deep purple), status pill, plan title, renewal date, 2-col stats (next charge + member-for). Billing NavRows (payment, history, switch yearly) → Stripe Portal. Cancel BottomSheet. Free: upgrade card. Fetches `GET /api/subscription`, portal via `POST /api/stripe/portal` |
| `AdminSidebar` | Admin sidebar nav (client): 240px fixed left, gradient logo + "ADMIN" badge, SVG icon nav (7 items: Home, Users, Edit, **Doc/บทความ**, Heart, Sparkle, AI), peach active border + folder-tab active state, version+email footer. NAV items carry optional `activePrefix` (บทความ = `/admin/article`, lights up on both articles + article-categories) and `dividerBefore` (above Mood Packs) |
| `AdminStatCard` | Admin KPI card: eyebrow label (11px uppercase), large value (26px 800), optional delta with color (+green/-red), optional sub text |
| `AdminBarChart` | SVG bar chart: stacked multi-series bars, title + legend, responsive viewBox. Used for DAU and AI usage charts |
| `AdminBadge` | Status/type badge pill: variants (premium/free/admin/success/warning/danger/info/neutral) with preset colors |
| `AdminPageHeader` | Page header: title (24px 800) + optional subtitle + optional action buttons slot |
| `admin-ui.ts` | Shared style constants: card, cardFlush, th, td, buttons (primary/ink/ghost/danger/small), statGrid, filterBar, input, eyebrow. COLORS semantic palette |
| `admin-icons.tsx` | 13 SVG icon components for admin nav + actions (Home, Users, Edit, Heart, Sparkle, Ai, Bolt, Dots, Search, Download, Filter, Check, X) |
| `DataTable` | Admin generic typed table: columns with optional render fn, zebra striping, page number pagination, rowKey, emptyText, pending opacity. Uses `A.th`/`A.td` from admin-ui |
| `OverviewShell` | Admin dashboard: 4 AdminStatCard KPIs (users/premium/revenue/AI) + DAU AdminBarChart (30d) + recent feedback list |
| `UsersShell` | Admin users: search + select filter, DataTable with avatar circles + AdminBadge plan pills + delete. URL-driven pagination |
| `UserDetailShell` | Admin user detail: back link, info card (toggle premium + delete buttons), info grid, stat cards, recent entries table |
| `EntriesShell` | Admin entries: DataTable with AdminBadge AI source, mood emoji+label, user link. userId filter with clear button |
| `AiUsageShell` | Admin AI usage: AdminBarChart (SVG stacked, purple NLP + peach Vision) + top users leaderboard (ranked email links) |
| `FeedbackShell` | Admin feedback: status filter tabs, feedback items (type emoji + rating stars + AdminBadge status + archive action), suggestion aggregation panel |
| `PacksShell` | Admin mood packs: 3-col grid cards with icon preview + AdminBadge tier, create form, edit inline, SVG upload to R2 |
| `NotFoundCharacter` / `ForbiddenCharacter` | Pure CSS+SVG kawaii circle characters for error pages. 404: blue circle with dot eyes + smile + peach "?" badges. 403: orange circle with closed eyes + blush + purple lock badge + sparkles. Both use `pop` animation on badges |
| `NotFoundPage` | 404 page client component: standalone layout (back arrow + header), character + faded "404" text, copy section, home + report buttons. Uses `useTranslations("errors")` |
| `ForbiddenPage` | 403 page client component: standalone layout, character + faded "403" text, "PRIVATE ENTRY" label, copy, sign-in + back buttons. Uses `useTranslations("errors")` |
| `PrivacyPage` | Privacy policy page: hero card (purple gradient + shield emoji + badge pills), TL;DR summary card (4 icon rows), 7 numbered sections with `**bold**` rendering. Uses `useTranslations("privacy")` |
| `TermsPage` | Terms of service page: hero card (peach gradient + handshake emoji + effective date), TL;DR summary card (4 icon rows), 7 numbered sections. Uses `useTranslations("terms")` |
| `PersonalEventsManager` | CRUD manager for user's personal important dates (settings page). Emoji input (48px) + label + month/day selects + purple add button. Event list with emoji tile + label + date. Free: max 3 + premium teaser. Premium: unlimited |
| `SpecialDayBanner` | Inline banner for today's holidays/personal events. Renders colored chips: holiday `#FFF0F3`/`#BE123C`, personal `#EFF6FF`/`#1D4ED8`. Used on Home hero and DaySheet |
| `ShareCard` | Shareable mood card rendered as a real 1200×630 DOM node (Open Graph ratio), captured to PNG client-side. Two templates: `streak` (🔥 + giant number + encouragement line) and `signature` (top mood emoji + label + %, full-width mood-color proportion bar, top-4 legend). Light/Dark theme via `palette()`. Bottom-right watermark (`BrandMark` gradient smiley + "DailyMood" / "dailymood.me"). **Hardcoded HEX only** (mood colors from `DEFAULT_MOODS`, no CSS vars) so it's theme-independent; **no remote images** (avoids canvas taint). `forwardRef` so the modal can target the unscaled node for capture |
| `ShareCardModal` | Bottom-sheet modal opened from Stats "แชร์" button: template tabs (Streak/Mood mix), Light/Dark segmented toggle, live scaled preview (full-res card scaled via `transform` + `ResizeObserver`), actions Share/Save/Copy. Capture via `html-to-image` `toBlob` (double-pass for Safari font readiness). Share = Web Share API with PNG file (falls back to text+url, then to download); Save = download PNG; Copy = `ClipboardItem` image. Privacy note: "no notes or personal details" |
| `ArticlesShell` | `/articles` overview in the "Paper Desk · Magazine grid" aesthetic. White paper-sheet cards on the normal themed body background (`.pa-wrap` has no surface fill; `color: var(--ink)`). Header: `.pa-chip` eyebrow + themed `<h1>` with `PAMark` peach highlight (highlighted word forced dark for contrast on peach) + `.pa-filter` category pills (ink active) + search toggle. Featured folder (`.pa-tab` + `.pa-sheet`, PAClip, cover, repurposed footer = `PASticker` + date + `.pa-btn.purple`) at 1.35fr; 3-card side stack (washi-taped `132px 1fr` rows, alt rotation) at 1fr; remaining articles in `.pa-rest-grid`. Covers via `<Cover>` (photo else `ArticleArt`). `.pa-card-lift` hover straighten+raise |
| `paper/*` primitives | `src/components/paper/`: `ArticleArt` (6 tone-based inline-SVG covers, `slice` fill, no remote images), `PAClip` (paperclip SVG), `PASticker` (tone→mood disc using real `moodIconUrl` face), `PAMark` (marker-pen highlight), `tone.ts` (`TONE_HUE`/`TONE_BG`/`TONE_MOOD` maps + `asTone`). All `.pa-*` CSS lives in `globals.css` scoped under `.pa-wrap`; the paper surface tokens (`--w-ink*`, `--w-rule*`, `--w-tint`, `--w-surface`, `--w-surface-2`, `--w-ai-grad`, `--w-chip`) are defined in `:root`/`.dark` and **alias the themed palette** (`--ink`/`--surface`/`--hairline`/`--hero-grad`…), so paper sheets now **flip with the app theme** — light cream in light mode, dark surfaces in dark mode (no more bright cards glaring on a dark page). In light mode the values are identical to the original hardcoded paper colours. **Dark-mode caveats:** "inverted ink" chips (`background: var(--w-ink)` — active `.pa-filter`/`.pa-tab.ink`/`.pa-btn.ink`/`.pa-icon-btn.active`) pair with `color: var(--bg)` so the pair flips together; white pills on fixed colored banners/photos (trial-promo bar, article cover badge) keep dark text via literal hex since their background never themes. Two more token families flip with theme: `--w-card-warm` (the warm "SUGGESTION"/notice sheet — cream in light, warm-dark in dark) and the semantic callout tints `--w-tint-{danger,info,success}` (+ matching `-fg` text) for error/info/success boxes & icon circles (light pastel → dark translucent). Vibrant peach→purple brand gradients with white text are left as literal hex (read fine on both themes); the Year-in-Pixels print/export window and the theme-picker preview swatches intentionally stay light |
| `ArticleDetailShell` | `/articles/[slug]` reader in the "Paper Desk" aesthetic — wrapped in `.pa-wrap` (white paper folders on the normal themed body background, **no** cream surface — same as the list page). Loose on-background text (back link, meta, `<h1>`, subtitle) uses theme-adaptive `--ink*`; text inside the white `.pa-sheet` cards / plum card / pills uses the scoped `--w-ink*`. Top bar = paper-pill save/share buttons (inline heart/share SVGs). Header = meta line + `<h1>` + subtitle (no per-article keyword to `PAMark`, so plain h1). Hero = tone-colored `.pa-tab` (category label) over a `.pa-sheet` with `PAClip` + real cover photo (else `ArticleArt`) + corner `PASticker` (tone→mood). Body via `<ArticleBody variant="paper">`. Outro = `.pa-tab.ink` + plum-gradient `.pa-sheet` (radial glow + white sticker) carrying the AI key-takeaway / guest walls. **New** post-read mood-reaction card (`.pa-sheet` + clip + 5 `PASticker` buttons → existing `/reaction` endpoint + analytics; guests get visual-only feedback). Sidebar (sticky): TOC folder (`.pa-tab.ink`) with a **real** scroll-progress bar (`progress` state), Related folder (`.pa-tab.mint`). Journal-prompt card intentionally omitted (no backend). All text ≥14px |
| `ArticleBody` (`variant`) | Markdown→JSX renderer (prose + numbered tip-cards). `variant="default"` (admin preview) keeps the themed card; `variant="paper"` (detail page) renders each numbered tip as a `.pa-sheet` folder with alternating washi tape + chunky tone-colored number badge. Section `id`s preserved for TOC scroll-spy. `.pa-wrap .article-prose` re-tones prose colors to the paper ink tokens |

## Smart Log Flow (UX)

1. User taps a mood on home row (or AI composer card, or FAB +) → modal opens with mood pre-selected
2. "How was your day?" heading + mood picker grid + note textarea (purple-bordered)
3. **Quick Save** (no text) — saves mood only as manual entry
4. **AI Analyze** (with text) — Gemini returns mood + tags → gradient detection panel with animated tag pills
5. User edits mood, removes/adds tags, then taps **Save** → entry appears in timeline

## Design Decisions Log

| Date | Decision | Reason |
|------|----------|--------|
| 2026-05-02 | Modern + Vivid style | ผู้ใช้เลือก |
| 2026-05-02 | Mobile-first | เผื่อ mobile app ในอนาคต |
| 2026-05-05 | AI confirm flow (suggest → edit → save) | ลด false positives จาก NLP |
| 2026-05-05 | Multi-entry/day timeline | สอดคล้องกับ smart logging |
| 2026-05-05 | Client-side image optimize (WebP, max 1600px) | ลด Worker CPU + bandwidth |
| 2026-05-05 | Signed read URLs for R2 (1h TTL) | Privacy ของรูปผู้ใช้ |
| 2026-05-06 | Default mood icons → custom SVG pack hosted on R2 | Unified brand-curated illustration set |
| 2026-05-07 | **Moodly redesign** — drop Clean Wellness → Moodly style. Urbanist font, purple/mint/peach palette, floating pill nav, streak strip, AI composer card. Based on Moodly Figma reference | ผู้ใช้ซื้อ Moodly Figma และต้องการ redesign ทั้งหมดให้เหมือน reference |
| 2026-05-07 | Mood colors: peach=happy, mint=calm, yellow=neutral, blue=sad, lavender=anxious, purple=tired | จับคู่กับ Moodly palette |
| 2026-05-07 | AI Composer card on home (not modal) with caret animation | Centerpiece UX — AI ควรเด่นชัดบนหน้า Home |
| 2026-05-07 | Implement all 10 design screens from Claude Design handoff | Calendar, Stats, AI Insights, History, Entry Detail — ทั้งหมดจาก Moodly design prototype |
| 2026-05-09 | Calendar Day Sheet (bottom sheet) | Daylio-style bottom sheet on day tap — keeps calendar context vs. full-screen push. Generic `BottomSheet` base extracted for reuse |
| 2026-05-09 | Calendar AI features (Summary + Patterns + Ask AI) | AI turns calendar from passive log to reflective surface. Single Gemini call per month for summary+patterns, cached in D1. Premium gating: free sees 1st sentence + blur + locked states. Pattern rings overlay on grid. Ask AI bar at bottom with NL search |
| 2026-05-09 | Timeline view merged into Calendar tab | Two views of one dataset via segmented control. Reuses stats-shell segmented control pattern (#F4F2F7 track, white pill). Filter chips = mood types (not tags). Entry title = aiSummary ?? first line of note ?? mood label. Separate `/api/calendar/timeline` endpoint (no signed URLs = faster). `/history` redirects |
| 2026-05-09 | Stats page redesign — functional period toggle + real activity impact | Period toggle now fetches different data windows (7/30/365d). Activity impact uses real tag-mood correlation from entries (no Gemini, pure data). "Best Day" → "Highest Mood Day" (copy guardrails). Delta badge computed server-side vs previous period. Year toggle premium-gated. Bottom nav: Stats replaces Insights tab |
| 2026-05-09 | AI Insights redesign — caching + feedback + hero actions | Weekly D1 cache (delta-3 invalidation like calendar AI). Suggestion feedback persisted in `suggestion_feedback` table. Hero card gets Read Full + Share. Pattern cards get mini sparkline SVG. Free tier: preview headline + locked state (data stripped at API level). Stats → Insights via link card |
| 2026-05-10 | Profile tab ("You") — 4 screens | Profile Overview replaces old Settings as bottom nav destination. Hero card with user-selectable accent color (6 options) driving gradient. Mood signature computed from 30-day distribution. Achievements with 12 badges, auto-earned on API check. Settings grouped into Reminders/Appearance/Language/Privacy/Data sections. `/settings` redirects to `/profile/settings` |
| 2026-06-03 | Social Share Cards rendered client-side (`html-to-image`), NOT `next/og`/Satori | Spike proved Satori drops Thai tone marks over upper vowels (หนึ่ง→หนึง, ที่นี่→ทีนี) — unacceptable for brand-quality cards. Browser DOM capture shapes Thai+emoji correctly. Cards use hardcoded HEX (theme-independent) + no remote images (no canvas taint). Free for all tiers (growth tool w/ watermark). MVP = Download/Share/Copy; deferred X auto-unfurl `/s/[shareId]` (needs server headless browser) + Highlight Entry template (needs note-blur) |
| 2026-05-10 | Subscription Management (`/profile/subscription`) | PRD adapted from iOS StoreKit → web Stripe. All billing actions via Stripe Customer Portal (no custom billing UI). Deep purple hero card distinct from profile's accent-based gradient. Subscription state stored in DB via webhook (stripeSubscriptionId, currentPeriodEnd, cancelAtPeriodEnd, planInterval) for fast reads. Cancel confirmation via reusable BottomSheet. Free users see upgrade CTA (never hidden per premium-gating rule). Profile NavRow links premium→subscription, free→pricing |
| 2026-05-10 | Admin Panel (`/admin`) — 5 pages | Separate layout outside `[locale]` — sidebar dashboard style, no TopBar/BottomNav. Auth via `ADMIN_EMAILS` env var (hardcoded email, no DB schema change). Server Components + Server Actions (no admin API routes). TH-only UI. Reuses same CSS variables and Tailwind. Dashboard → Users (CRUD) → Entries (browse/delete) → AI Usage (charts) → Feedback (user + AI suggestion) |
| 2026-05-11 | 404 + 403 error pages — kawaii character style | Standalone pages (no TopBar/BottomNav) matching Moodly Figma reference. Pure CSS+SVG characters (no image assets). 404: blue circle + "?" badges, reassuring copy. 403: orange circle + lock badge + sparkles, private entry context. Root `not-found.tsx` wraps own `NextIntlClientProvider`. 403 at `/forbidden` inside `[locale]` tree. "Report broken link" links to existing profile feedback feature |
| 2026-05-11 | Privacy + Terms legal pages | Human-readable legal pages matching Moodly design. Same structure: gradient hero card (purple for privacy, peach for terms), TL;DR summary with icon rows, numbered detail sections. Content adapted to DailyMood's actual stack (Cloudflare D1/R2, Gemini, Stripe, Resend). No auth required. Linked from Settings About section (NavRow with href) + Pricing footer. Bilingual TH/EN via i18n |
| 2026-05-13 | Edit Entry redesign — 2-col desktop layout | Matching Figma reference with form left + live preview sidebar right. Horizontal mood picker row (not grid toggle). Period selector auto-derived from time input. Bold toolbar uses **markdown** markers (no rich text dep). Real keyboard shortcuts (⌘S/Esc/⌘↵). Sidebar is sticky. Mobile: sidebar hidden, fixed bottom bar. Image upload teaser shown for free users (fixes premium-gating rule violation) |
| 2026-05-16 | Avatar upload on Edit Profile | Premium-only. Camera FAB wired to file input (accept=image/*). Client-side optimize via `optimizeImage()` → POST `/api/profile/avatar`. R2 key `users/{userId}/avatar/{ulid}.webp`, old avatar deleted on re-upload (after DB commit to prevent orphan). `users.imageKey` column stores R2 key; `users.image` kept for Google OAuth URL. Render priority: imageKey (signed URL) > image (Google) > initials. TopBar server component queries DB for imageKey to show fresh avatar everywhere. Remove button resets to Google photo or initials (DELETE endpoint, no premium gate). `router.refresh()` after upload/remove to invalidate server-rendered avatar. Blob URLs revoked via cleanup ref to prevent memory leaks |
| 2026-05-16 | Edit Entry mobile refinement | Bottom bar was invisible on mobile because parent `.fade-in` uses CSS `transform` (animation), which creates a containing block that breaks `position:fixed` for descendants. Fix: render bottom bar + delete modal via `createPortal(document.body)`. Layout's `<main z-index:0>` creates a stacking context that traps `position:fixed` descendants below the BottomNav (z-index:40) regardless of their own z-index — the portal escape solves this too. Position the bottom bar at `bottom:104px` to sit above the BottomNav rather than overlap. Sidebar's inline `display:flex` needed `!important` in CSS to be overridden by `display:none` on mobile. Danger zone (delete entry) duplicated below the form via `edit-mobile-only` class because the sidebar is hidden on mobile — without it, users couldn't delete entries on small screens |
| 2026-05-16 | Site-wide copy rewrite — formal but natural Thai | Pass over `messages/th.json` + `messages/en.json` to remove translated/AI-generated feel without going chatty. Removed `เล่า`/`บอก` asks per CLAUDE.md tone rule, dropped overly casual particles (`นะ`, `ลอง...`, `อีกสักหน่อย`), smoothed choppy bullet-style sentences into natural Thai. English: tightened chatty phrasing to polished but warm. Privacy + Terms: kept all technical content (Cloudflare R2, TLS, signed URLs, Google Gemini, Stripe, Resend, OAuth, AI cache, hotlink, uptime) — only the surrounding language was reformed |
| 2026-05-24 | AI Flashback & Cognitive Reflection | Flashback card on Entry Detail page for negative moods (score ≤ 2). Gemini searches past entries with similar emotions, generates 2-3 sentence reflection with **bold** key phrases referencing how user overcame a similar situation before. Blue gradient card (#F0F7FF→#E8F4FD→#F5F0FF) with clock icon + "ย้อนดูตัวเอง" header, past date reference, AI disclaimer. Free tier: dashed teaser card with 🕰️ emoji + upgrade CTA. No cache — on-demand generation via extended `GET /api/log/[id]`. Triggers only for sad/angry/anxious/tired moods. Premium only |
| 2026-05-22 | AI Chart Annotations | Glowing AI-annotated pins on mood trend SVG chart. Gemini analyzes moodTrend + tags to detect 2-5 anomalies/highlights per period. Pins use SVG `<animate>` for pulse glow effect + `<filter>` for blur. Tooltip rendered as HTML div overlay (not SVG title/foreignObject) to avoid clipping — positioned via viewBox→screen coordinate projection using `getBoundingClientRect()`. Free tier: 1 ghost pin with blur filter + upgrade CTA pill. On-demand generation with DB cache (`chart_annotations_cache`, delta-3 invalidation). All periods (week 7pts, month 30pts, year 12pts). Extends existing `/api/stats` response — no separate endpoint |
| 2026-05-22 | AI Guided Journaling Prompts | SmartLogModal textarea placeholder now changes based on selected mood. Free: static pre-written prompts per mood (7 moods × TH+EN, instant). Premium: Gemini-generated personalized prompt using recent tags/moods as context, cached per user/mood/date in `journal_prompt_cache` table for daily variety without redundant calls. Custom moods (Premium): Gemini adapts using mood label. UX: placeholder updates silently — no spinner, user can type immediately, personalized prompt fades in if Gemini resolves first. AbortController prevents stale prompts on rapid mood switching. Follows CLAUDE.md tone: no "เล่า"/"บอก", gentle/open. API: `GET /api/ai/journal-prompt`, no NLP quota impact |
| 2026-05-18 | Location picker on mood entries | Optional location field via `LocationPicker` component. Pin icon + text input (Google Places Autocomplete) + GPS crosshair button in a single row matching existing field container pattern (`var(--surface)` bg, `var(--hairline)` border, `borderRadius: 14`). Google Maps SDK lazy-loaded via `@googlemaps/js-api-loader` only when component mounts (not in layout.tsx). GPS: browser Geolocation → Google Geocoder reverse lookup → "City, Country" string. Clear button (×) when value present. GPS loading uses existing `ai-spin` CSS animation for the spinner. GPS error shown inline in `#D14343`. Placed below mic/photo toolbar in SmartLogModal, as labeled section in EditEntryShell. Display: pin icon + text in `var(--surface)` pill on EntryDetail, one-line truncated in TimelineFeed EntryCard and DaySheet mini card. All tiers (no premium gate). Graceful degradation: component returns null if `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` is not set |
| 2026-05-24 | Public Holidays + Personal Events | Calendar grid: top-left 7px dot indicator — red `#F43F5E` for holidays, blue `#3B82F6` for personal events. Distinct from existing AI pattern dots (bottom-center). DaySheet: colored chip row above entries — holiday chip: `#FFF0F3` bg + `#BE123C` text, personal chip: `#EFF6FF` bg + `#1D4ED8` text, pill-shaped with emoji prefix. Home hero: `SpecialDayBanner` renders between greeting subtitle and h1, same chip style. Profile Settings: "วันสำคัญ" section after Custom Moods, `PersonalEventsManager` component (emoji input 48px + label input + month/day selects, purple add button, event list with date subtitle). Free: 3 events max with premium teaser card (gradient `#FAF7FE→#FDE8DA`, ✦ PREMIUM badge). Holiday data: Nager.Date API cached in `holiday_cache` table (30-day TTL) |
| 2026-06-02 | Theme toggle on desktop topbar | The light/dark `ThemeToggle` (36px square, `var(--surface)` bg + `var(--hairline)` border, sun/moon SVG) previously only appeared in the mobile topbar — added it to the desktop topbar action row, placed between the "+ บันทึก" CTA and the 🌐 language toggle. Reuses the existing `ThemeToggle` component; `TopBarClient` now calls `useTheme()` and derives `isDark` (same logic as mobile, with `auto` resolved via `matchMedia`). Click toggles light↔dark and persists to `localStorage` |
| 2026-06-02 | SmartLogModal — show selected mood | When a user tapped a mood on the home page, the modal opened without surfacing it, so users were confused about whether their pick registered. Added an always-visible "อารมณ์ของคุณ / Your mood" row at the top of the form: horizontal scroll of mood chips (icon 34px + label stacked, `minWidth 68`), selected chip highlighted (2px `var(--ink)` border + `var(--surface)` bg vs `var(--surface-2)`). Tappable to change the mood before writing. Hidden once an AI suggestion is shown (which has its own mood picker). Also fixed the premium AI journal-prompt generator: it could emit "เล่า/บอก" mid/end-sentence (e.g. "ลองเล่าให้ฟังหน่อยนะ") — the rule now forbids those words in any position and avoids the trailing "นะ" particle (tone rule). Cached prompts regenerate daily |
| 2026-06-02 | Home AI composer — show summary | The inline "AI MOOD ASSISTANT" composer on Home captured & saved the AI summary but never displayed it (only mood pills + tags showed), so the AI's analysis text appeared to vanish after analysis. Added a "สรุป / Summary" card after the tag pills (shown when `composerSuggestion.aiSummary` exists — premium): `var(--surface-2)` bg, purple 14px bold label, `**bold**`→`<b>` rendering — matching the SmartLogModal summary block |
| 2026-06-05 | Article reactions — consumers (admin + user) | Made the post-read mood reaction data actually useful (it was only being stored). **Admin:** article editor (`/admin/articles/[id]`) now shows a reader-reaction breakdown card — per-mood count + % bar (in each mood's colour) + total, aggregated via `GET /api/admin/articles/[id]` → `reactionCounts`. **User:** new `/profile/article-reactions` page (`ArticleReactionsShell` + `GET /api/articles/reactions`) listing articles the user reacted to with the mood they picked, linked from a profile sidebar nav row. Both read the `article_reactions` table; no schema change. Followed existing conventions: admin uses `MoodIcon`/`DEFAULT_MOODS`/local `CARD`; user page clones the saved-articles shell pattern (inline TH/EN, `l()` helper, `.card` rows). |
| 2026-06-04 | Article detail → "Paper Desk · Mood Reader" | Reskinned `/articles/[slug]` to match the paper-desk list page. Ported from design handoff `docs/design_handoff_article_v2_paper/` (the ★ V2 mood-integrated reader artboard). Reuses the existing `paper/*` primitives + `.pa-*` CSS — mostly composition, no new primitives. Decisions vs the mock: the mock's warm cream desk + grain was dropped after review — paper folders sit on the normal themed body background, identical to the list page (loose text uses theme-adaptive `--ink*`, white-sheet text uses scoped `--w-ink*`, so both light/dark work); hero shows the real cover photo (else tone `ArticleArt`) wrapped in the folder, not the generic `HeroArt` sunrise; hero folder tab = category label (no per-article "featured" flag), header h1 left plain (no CMS keyword to highlight); author/byline absent from schema so dropped. **New UI** rendered for the first time: the post-read mood-reaction row (handler/endpoint/analytics already existed but were never in the JSX) and a real reading-progress bar (the `progress` state existed but was never shown). The "save as journal prompt" sidebar card was **skipped** (no backend). `ArticleBody` gained an opt-in `variant="paper"` so the admin preview is untouched. All detail-page text kept ≥14px (min-font rule). No cream desk (`.pa-desk` dropped) — paper folders on the themed bg. **Follow-up:** wired real reaction persistence — new `article_reactions` table (`userId`+`articleId` PK, `moodTypeId`, upsert on re-react; migration `drizzle-pg/0013`), the `/reaction` route now validates the mood against `DEFAULT_MOOD_IDS` and upserts, and `GET /api/articles/[slug]` returns the saved reaction so the sticker stays selected on reload |
| 2026-06-04 | Articles overview → "Paper Desk · Magazine grid" | Reskinned `/articles` to the paper-desk aesthetic (folder tabs, paperclips, washi tape, mood-face stickers, layered/rotated white paper sheets). Ported from design handoff `docs/design_handoff_articles_paper_grid/`. Decisions vs the mock (data the DB lacks): covers use the real photo if present else a tone-based `ArticleArt` SVG; mood-face sticker derived from `articles.tone` via `TONE_MOOD` (peach→amazing, lavender→anxious, mint→happy, yellow→neutral, blue→sad, purple→tired) rendered with the real `moodIconUrl` face; featured footer **repurposed** (no author/role columns) to sticker + date + read-more button; full-page scope (featured + 3 side stack + paper rest-grid) keeping live search + dynamic category pills. `/api/articles` now returns `tone`. **Post-review tweaks:** dropped the warm cream surface/grain — cards now sit on the normal themed body background; removed the floating decorative sticker that overlapped the filter pills; featured read-more button changed from harsh black ink → brand purple (`.pa-btn.purple`). New: `src/components/paper/*` (ArticleArt, PAClip, PASticker, PAMark, tone.ts), `.pa-*` styles in `globals.css`, `titlePre/titleMark/titlePost` i18n keys for the peach-marker heading. **Fix (2026-06-06):** the featured folder's `overflow:hidden` clipped the overhanging `PAClip` (cut off at the sheet's top edge) → changed to `overflow:visible`; safe because the inner cover self-clips via its own `margin:14` + `borderRadius:14` + `overflow:hidden`, so the sheet's rounded corners stay clean. (Side-stack + rest-grid cards keep `overflow:hidden` — their covers bleed to the edge and have no overhanging clip.) |
| 2026-06-05 | Today dashboard + Smart Log modal → "Paper Desk" | Reskinned the authenticated home (`home-shell.tsx`) and the Smart Log AI modal (`smart-log-modal.tsx`) from `docs/design_handoff_today_smartlog/`. **Logic untouched** — same `/api/log/smart`→`/api/log/confirm` flow, `/api/ai/remaining` quota (real **3/day** free, NOT the mock's "10"), Gemini, voice/photo/location/activities, premium gating via `tier`. Reuses existing `paper/*` primitives + `.pa-*` CSS. **Decomposition (Approach C):** modal fully extracted into `paper/smartlog/` (`SLShell` + `SLHeader` + `SLInput`/`SLAnalyzing`/`SLResult`/`SLRateLimit`); dashboard panels into `paper/today/` (`GreetingFolder`, `TodayTimeline`, `EntryFolderCard`, `AiWeeklyFolder`, `StreakCard`, `MiniCalendarFolder`) but the AI composer kept inline in `HomeShell` (state too interconnected to thread cleanly). **`SLShell` adds the dialog a11y the old modal lacked** — `role=dialog aria-modal`, Esc + backdrop close, focus trap, focus restore, scroll-lock; carries `.pa-wrap` so scoped `--w-*` tokens resolve through the `createPortal`. New **dedicated full-screen "analyzing" state** (spinner ring + 🧠 + step chips) replacing the old inline block; ring respects `prefers-reduced-motion`. New `PaperIconButton` primitive (mic/photo/location). `PASticker` gained optional `pack`/`iconFormat`/`iconKey` (backward-compatible — articles still calls it bare) so picker + entry cards render the user's real pack + custom-mood icons. **DIVERGENCE from the articles decision:** the articles reskin dropped the cream desk; this PRD re-specifies it, so Today uses a `.pa-desk` cream surface + dot grain (scoped modifier — articles untouched) → Today is an always-light "paper island" even in dark mode (the modal/`.pa-wrap` are also always-light by design). To match articles instead, remove the `pa-desk` class from the `HomeShell` root. New `TrialPromoBar` (gradient strip above `TopBar`, gated server-side to non-premium + no active trial, session-dismissible) wired in `[locale]/layout.tsx`. New `--w-tint` token (`#F4EEE6`) defined on `.pa-wrap` (was referenced by the handoff but never existed). |
| 2026-06-06 | Fix double trial banner | Two trial-offer banners stacked for free, never-trialed users: the dismissible `TrialPromoBar` (layout, → /pricing) **and** `TrialBanner mode="activate"` (TopBar, one-click trial via `TrialConfirmSheet`). Merged into one: `TrialPromoBar` keeps the dismiss ✕ but its CTA now opens `TrialConfirmSheet` (one-click 14-day trial) instead of linking to /pricing; removed the `activate` branch from `TopBar` (the countdown banner for active trials stays). Also tightened the promo gate in `[locale]/layout.tsx` to `!isPremium && !trialActive && !trialActivatedAt` so the "free trial" offer only shows to users who can actually start one (was showing to expired-trial users too). |
| 2026-06-06 | Fix language switch (EN unreachable) | The EN/TH toggle set `NEXT_LOCALE` + reloaded but the app stayed Thai. Root cause: commit `8830e28 "force Thai locale"` set `localeDetection: false` in `routing.ts`. In next-intl v4 that flag disables **cookie** detection too (not just accept-language) — `resolveLocale` only reads the cookie when `localeDetection` is truthy — so the middleware always resolved `defaultLocale` (th) and, because `localePrefix: "never"`, that resolved locale also drove the internal `[locale]` rewrite. Worse, `syncCookie` then **overwrote** the `en` cookie back to `th` on every response. Fix: `localeDetection: true` (cookie honored, not clobbered) + a thin `middleware.ts` wrapper that injects a default `th` `NEXT_LOCALE` cookie when none exists, so next-intl never falls back to the browser's `accept-language` — Thai-first is preserved, switching works. Also hardened the toggle to reload via `.finally` so a failed `/api/profile` PATCH can't block the switch (the cookie drives locale). Verified: no-cookie→th, en-cookie→en, th-cookie→th, en-browser-no-cookie→th, no Set-Cookie clobber. |
| 2026-06-06 | Real brand logo (header + share card) | Replaced the hand-drawn `DMLogo` SVG with the real brand mark from `assets/logo.png` (R2). Used the **mark only** (gradient smiley) + kept the "DailyMood" wordmark as themed text, rather than the full lockup, because the lockup's wordmark is dark ink and would vanish on the dark topbar and the dark share card. Inlined the mark as a base64 data-URI (`brand-logo.ts` → `BrandMark`) instead of referencing the R2 URL: the share card is captured with `html-to-image`, where a cross-origin image taints the canvas (existing share-card constraint). A data-URI is canvas-safe and lets the same mark serve the header (DOM) and the watermark. Mark was cropped + optimized from the 249×80 lockup to ~64px (~5.9KB PNG). Swapped into both topbar `DMLogo` defs and the share-card `Watermark` (dropped the purple sparkle tile). |
| 2026-06-06 | Achievement detail + social share | Made every badge on `/profile/achievements` tappable → a Paper Desk detail sheet, and let users share **earned** achievements to social. Deliberately **mirrored the proven Stats share pattern** rather than inventing a new one: a dedicated `AchievementShareCard` (1200×630, hardcoded HEX, no remote images, light/dark) captured client-side via `html-to-image`, with the same Web Share → clipboard → download fallbacks. Extracted that capture/share/copy/download flow into a reusable `useCardShare` hook and exported `palette`/`mix`/`Watermark`/`THAI_FONT` from `share-card.tsx` (additive — the existing Stats `ShareCardModal` was left untouched to avoid regressing a shipped feature; it still carries its own inline copy). Detail view reuses the generic `BottomSheet` for portal/scrim/Esc/a11y with bespoke Paper Desk content inside (best of both). All states open the detail (in-progress shows progress + "X to go", locked teaches how to unlock — consistent with the no-hiding principle); only earned shows Share. **Free for all tiers** (growth tool w/ watermark, matching the mood cards). Share-card/modal copy branches inline on `locale` (share-card convention); in-app sheet labels branch inline `th ? … : …` (matching `achievements-shell.tsx`'s existing copy) — no `messages/*.json` churn. Analytics `trackShareAchievement(badgeId)` (mirrors `trackArticleShare`). |
| 2026-06-06 | Achievements → "Paper Desk · Sticker Album" | Reskinned `/profile/achievements` to the paper system. Reframed achievements as a **sticker collection** — the strongest paper-desk metaphor (badges literally are stickers). Three states encoded through paper *materials*, not just colour: earned = vivid sticker disc + washi tape + a tilted rubber-stamp date; in-progress = pale disc + real paperclip + paper progress meter; locked = flat faded grayscale disc on a **dashed empty slot** (kept title + desc visible so it teaches what unlocks it — consistent with the no-hiding principle). Left rail = sticky Progress folder (`pa-tab` + sheet + paper-toned ring) + `pa-filter` pills. **Conventions match entry/article detail:** `.pa-wrap` on the themed body background (NO `.pa-desk` cream island — keeps dark mode working), loose text uses `--ink*`, white-sheet text uses scoped `--w-ink*`. Reused existing `paper/*` primitives (`PAClip`, `PAMark`, `.pa-*` CSS) — no new primitives; `BadgeSticker`/`BadgeStickerCard` kept local to the shell. Data, API (`/api/profile/achievements`), and badge catalog untouched. Replaced the old `.achievements-*` CSS with `.ach-*` (paper grid + sticky rail + responsive ring). All text ≥14px (overrode shared `pa-chip`/`pa-filter` 12–13px to 14px inline to honour the min-font rule). |
| 2026-06-05 | Entry detail → "Paper Desk" | Reskinned `/entry/[id]` (`entry-detail.tsx`) to match the Today/Articles paper system — no separate handoff, followed the established `.pa-*` primitives. Mood **hero folder** (date tab + `PAClip` + `PASticker` + big date) replaces the old mood-tinted gradient hero; left column = washi-taped Note sheet, AI-insight + Flashback as tinted washi sheets (reusing the SmartLog-result treatment), paperclipped photo frame + Maps sheet, `--w-tint` tag pills; right rail = `pa-tab` folder cards (nearby-days timeline now uses `PASticker`, last-month, washi-taped streak). **Conventions (same as article detail):** paper folders sit on the themed body background (no `.pa-desk` cream — the global body grain shows through), white-sheet text uses scoped `--w-ink*`, top-bar/loose text uses theme-adaptive `--ink*` so dark mode works; back/edit rendered as paper pills. Logic, data, and `GET /api/log/[id]` untouched. The AI-summary `dangerouslySetInnerHTML` `**bold**`→`<b>` pattern was carried over verbatim (pre-existing). |
| 2026-06-05 | Edit entry → "Paper Desk" | Reskinned `/entry/[id]/edit` (`edit-entry-shell.tsx`) to the paper system. **The whole left form sits on a single white `.pa-sheet`** (folder tab "แก้ไขบันทึก") — chosen over loose fields so the always-light `--w-ink*` text is dark-mode-safe and it reads as filling in a paper form. Paper mood tiles (mirroring the Smart Log input), `#FBF7F0` date/time + note inputs, toolbar buttons restyled (bold/voice/image + AI re-analyze as a lavender-tint chip), AI-suggestion + error as tinted insets, `.pa-chip` tags + dashed suggested pills. Right rail: live-preview **folder** (mint tab + mood-tinted glow + `PASticker` instead of the old flat mood-colored card), AI-insight washi sheet, shortcuts sheet, `pa-btn` Save + paper-outline Cancel, danger-zone sheet (kept red accent). Delete-confirm modal re-themed as a `.pa-wrap` paper sheet with a lavender washi. Mobile portal bottom bar Save → `pa-btn`. All state/handlers/keyboard-shortcuts and `PATCH/DELETE /api/log/[id]` untouched. Photo button for free users now links to `/pricing` (was an inert PRO pill). |
| 2026-06-05 | Calendar → "Paper Desk" (core) | Reskinned `/calendar` core (`calendar-shell.tsx` + `day-sheet.tsx`) to the paper system. Month grid now lives in a **paper folder** (month-name `.pa-tab` over a `.pa-sheet`), empty day cells use `--w-tint` and today's ring is `--purple` (was peach); view toggle = `.pa-filter` pills (Calendar/Timeline/Year); prev/next = `.pa-icon-btn`; right-rail stat cards + legend = paper sheets with colored left-accent; free AI upsell = tinted paper card with gradient sparkle. Day-sheet rendered on a `.pa-wrap` paper backdrop (dim `.46`/blur `6px` matching Smart Log) with paper card surfaces and mood `PASticker`s; empty-day CTA = `pa-btn`. White-sheet text `--w-ink*`, loose header text theme `--ink*` (dark-mode safe). Removed dead `StatCard`/`MONTH_SHORT_EN`/`daysInMonth`/`onNavigate` leftovers. All data/`/api/calendar*` flows unchanged. |
| 2026-06-05 | Calendar premium AI cards → "Paper Desk" | Finished the calendar's Premium-only AI surfaces so the page is cohesive top-to-bottom (the themed cards above the paper grid were the "still not good"). `calendar-ai-summary.tsx` → washi-taped tinted `.pa-sheet` (gradient sparkle square, white-inset highlight chips, `--w-ink*` text); `calendar-patterns-feed.tsx` → `.pa-sheet` pattern cards + dashed paper locked-state; `calendar-ask-ai.tsx` → paper input bar (`#fff`/`--w-rule`, purple-gradient submit), tinted free-upsell, `.pa-sheet` result card with `--w-tint` date chips, purple-strong accents. Logic/`/api/calendar/ai`+`/ask` untouched. |
| 2026-06-06 | Today empty state → "Paper Desk" | The Today feed's no-entries-today state was bare centered text (`emptyTitle` only, `--w-ink-3` 15px) floating on the dot grain. Replaced with a new `EmptyToday` component (`paper/today/empty-today.tsx`): a centered `.pa-sheet` with lavender washi tape, a soft lavender radial halo behind a floating mood `PASticker` (mint "happy" face, new `pa-float` keyframe — disabled under `prefers-reduced-motion`), `emptyTitle` heading + `emptyBody` subcopy, and a purple `.pa-btn` CTA that smooth-scrolls to and focuses the inline "AI MOOD ASSISTANT" composer above (`composerRef` added to its textarea). Copy reworked to the tone rule: `emptyBody` dropped the redundant "Smart Log" mention (the button is that path now) → TH "แตะอารมณ์ด้านบนเพื่อบันทึกความรู้สึกของวันนี้" / EN "Tap a mood above to capture how today feels."; new `emptyAction` key (TH "เริ่มบันทึกความรู้สึก" / EN "Start logging"). The CTA overrides `.pa-btn`'s fixed `height:42px`/zero vertical padding with `height:auto` + `11px 22px` padding + `lineHeight 1.3` so Thai upper vowel/tone marks aren't clipped (instance-scoped; shared `.pa-btn` untouched). Also widened the Today feed spacing on mobile: `.pa-entries-grid` gap 18→34px and `.pa-today-grid` (entries↔right-rail) gap 20→38px (desktop entries row-gap 18→26). Theme-aware (`--w-ink*`). No logic/data changes. |
| 2026-06-06 | Calendar right-rail stat cards → folder tabs | The 3 stat cards (Avg mood / Streak / Logged) used a `4px` coloured **left-border** on a plain `.pa-sheet` — looked like an alert box and didn't match the paper system. Reskinned to **folder-tab cards** (`calendar-shell.tsx`): each stat is now a `position:relative` wrapper with a coloured `.pa-tab` (peach default / `purple` / `mint`) above a `.pa-sheet`, matching the entry folders + month grid; label moved into the tab (Streak now localised TH "ต่อเนื่อง"), big number kept at 38px `--w-ink`. The Legend card gained an `ink` tab ("คำอธิบายสี") for rail consistency. Rail gap 14→22px. No data/logic changes. |
| 2026-06-05 | Calendar Timeline tab → "Paper Desk" | Reskinned `timeline-feed.tsx`: paper filter pills (ink active / white-shadowed inactive, keeping the mood-colour dot), day-group headers in `--w-ink*`, and the per-entry cards now **reuse the shared `EntryFolderCard`** (`paper/today/`) instead of a local card — so timeline entries get the same time-of-day folder tab + `PASticker` + rotated-paper treatment as the Today dashboard. Grid switched to `.pa-entries-grid` (3-col → 1 on ≤767px). Removed the local `EntryCard`/`SkeletonGroup` markup + the `moodIconUrl` import. `TimelineEntry` is structurally assignable to `EntryFolderCard`'s `TodayEntry`, so no mapping shim needed. Data/`/api/calendar/timeline` unchanged. |
| 2026-06-05 | Year in Pixels → "Paper Desk" | Reskinned `year-in-pixels-shell.tsx` (Premium page). View toggle → `.pa-filter` pills; year-nav → `.pa-filter`, Export PNG → `pa-btn ink`. The 12×31 pixel grid sits in a **paper folder** (`--w-tint` empty cells, purple today-ring, `--w-ink*` day/month labels, `--w-rule` legend divider). The AI year-summary card became a **washi-taped tinted `.pa-sheet`** (gradient sparkle badge, `rgba(255,255,255,.72)` white-inset stat chips + compare card) — note: because this card now uses a fixed light gradient, **all its text was moved to `--w-ink*`** (the old code relied on `--hero-grad` going dark in dark mode; the paper tint is always light, so theme `--ink*` would be invisible in dark mode). Selected-cell tooltip + bottom stat cards → paper sheets with `PASticker`. Free upsell → washi paper sheet + `pa-btn`. `getMoodColor` empty → `--w-tint` (export PNG hex check updated to match). Loose header text stays theme `--ink*`. All data/`/api/year-in-pixels`, PNG canvas export + PDF report untouched. |
| 2026-06-05 | Year Story → "Paper Desk" (full redesign) | `/year-in-pixels/story` (`year-story-shell.tsx`) fully rebuilt onto the paper system (first pass was a too-light touch — redone). Every section is now a folder/sheet inside `.ys-w` on the themed desk: **hero folder** (purple `★ YEAR STORY` tab + `PAClip` + lavender glow + big `--w-ink` year + `PAMark`-highlighted theme line + the weekly pixel strip), **two stat folders** (washi-taped, big `--w-ink` numbers), **dominant-mood folder** (lav tab + mood-tinted glow + `PASticker` + distribution bar), **best/tough month folders** (yellow/purple tabs, accent avg, AI quarter text), **patterns** as two paper sheets, the **AI narrative** as an ink-tab **plum folder** (`linear-gradient(155deg,#2A1F33,#1A1320)`, peach glow, white text), the every-day **pixel grid** as a mint-tab folder (`--w-tint` empties), and an **outro sheet** (`PASticker` + `pa-btn` back). All sheet text uses `--w-ink*` (dark-mode safe since sheets are always-light); `Reveal` scroll animations + data/`/api/year-in-pixels` untouched. Hero top padding reduced (was a ~180px void under the nav). |
| 2026-06-05 | Stats → "Paper Desk" | Reskinned `/stats` (`stats-shell.tsx`). Shared `CARD` style object flipped from themed surface+border to a white paper sheet (chunky warm shadow, radius 18) — so the trend chart, mood-mix, special-days and activity-impact cards all become paper; inner text moved to `--w-ink*`. SVG line-chart gridlines `var(--surface-3)`→`var(--w-tint)`, axis labels →`--w-ink-3`. Period toggle + share + year-unlock → `.pa-filter` pills / paper; AI-insight summary → washi-taped tinted `.pa-sheet`; 4 stat cards → `.pa-sheet`; too-few-entries + loading skeleton paper-ified (`pa-btn` CTAs). Header title kept theme `--ink*` (loose, dark-mode safe). Dead `MoodDonut`/`ActivityBar`/`LABEL` left as-is. All data/`/api/stats`/`/api/insights` + share-card flow untouched. |
| 2026-06-05 | Insights → "Paper Desk" | Reskinned `/insights` (`insights-shell.tsx`). Shared `CARD` style flipped to a white paper sheet (warm shadow, radius 18) → the 4-feature grid (Forecast/Mood DNA/Themes/Energy Clock), 3 pattern cards and footer toggle cards all become paper, inner text moved to `--w-ink*`. **Hero recap** rebuilt as a folder: purple `.pa-tab` ("Weekly summary · by AI", the old inline eyebrow) over the existing gradient sheet + `PAClip` overhang (removed `overflow:hidden` so the clip isn't cut), glass stat tiles kept (they sit on the colored gradient). Disclaimer banner → washi-taped tinted `.pa-sheet` (`#F1E7FA→#F8EDEB`); week-nav → `.pa-filter` pills; status "ready" pill → white paper chip + `--w-tint` quota track; suggestion card → washi-taped warm sheet (`#FFF6EA→#FDEFE0`); `FeatureTeaser`/`FeedbackPill`/`TogglePill` recolored to white/`--w-tint`/`--w-rule`. SVG RadarChart + EnergyBars + MoodBarChart strokes/labels → `--w-rule(-strong)`/`--w-ink-3`. `LockedCard` (`--hero-grad`→lav-washi tinted sheet), states + skeleton paper-ified (`pa-btn purple` retry), `FreeGate` CTA → purple PRO-tab folder + `PAClip`, blurred previews → `.pa-sheet`. Hardcoded `#A673F1` accents swapped to `--purple-strong`. Root wrapped in `.pa-wrap`; loose header text kept theme `--ink*` (dark-mode safe). `AiSubTabs` left theme-based (shared with not-yet-reskinned `/ask-ai`). All state/handlers + `/api/insights/all|feedback`, `/api/profile` flows untouched. |
| 2026-06-05 | Ask AI → "Paper Desk" | Reskinned `/ask-ai` chat (`ask-ai-shell.tsx`) + the shared `ai-sub-tabs.tsx`. The full-height chat keeps its themed `var(--bg)` desk + grain; distinct surfaces become paper: **AI answers** = white `.pa-sheet` cards (`4px 16px 16px 16px` folded corner) holding a `--purple-strong` eyebrow + `--w-ink` body + feedback row; **user turns** = lavender `#F1E7FA` bubbles (`--w-ink`); suggested-question buttons + the floating **input bar** (`#fff` + `--w-rule-strong`, purple-gradient ↑ send, `--w-tint` when idle) are paper; "New question" = `pa-btn ink`; history toggle = `.pa-filter`. Thread list stays loose-themed (`--ink*`, `--primary-bg` active + `--purple` left rule) since it sits on the themed sidebar. **FreeGate** rebuilt as a plum PRO-tab folder (`linear-gradient(135deg,#2C2435→#A673F1)` + `PAClip`) with `.pa-sheet` blurred previews. Root wrapped in `.pa-wrap`; all responsive class names (`ask-ai-wrap/-sidebar/-history-btn/-back-btn/-input-bar`) + chat logic/refs/localStorage + `/api/ask-ai/*` untouched. **`AiSubTabs`** (used only by `/insights` + `/ask-ai`, both now `.pa-wrap`) switched from theme pills to `.pa-filter` (white inactive / ink active) so the AI tabs match the rest of the desk. |
| 2026-06-05 | Pricing → "Paper Desk" | Reskinned `/pricing` (`pricing-shell.tsx`). Root `.pa-wrap`; the 6 feature cards + 2 plan cards became white `.pa-sheet` (feature cards `pa-card-lift`; active plan = `2.5px solid var(--purple)` ring + `--primary-bg`, radio + sub-price → `--purple`/`--purple-strong`, icon tiles → `--w-tint`). **Free-vs-Pro comparison** rebuilt as an ink-tab folder (`.pa-tab ink` "Free vs Pro" + `.pa-sheet` + overhanging `PAClip`), table rules → `--w-rule`, Pro column → `--purple-strong`, cell text → `--w-ink*`. **Trial CTA** → washi-taped (`lav`) lavender-tint sheet (`#F1E7FA→#F8EDEB`, `--w-ink*` text). **Cancelled banner** → warm tinted `.pa-sheet`. The brand **peach→purple Pro gradient** (`#FCA45B→#A673F1`) is kept as the premium accent on the hero badge + trial/subscribe CTAs, but the CTAs now carry the signature chunky offset shadow (`0 8px 0 -2px var(--purple-strong)…`) so they read as paper buttons; success state → `pa-btn ink`. Loose hero/footer text stays theme `--ink*`. Shared `TrialConfirmSheet` (generic `BottomSheet` modal) left themed — out of `.pa-wrap` scope. All Stripe/trial handlers + analytics tracking + `/api/stripe/checkout` untouched. |
| 2026-06-05 | Profile → "Paper Desk" | Reskinned `/profile` (`page.tsx` header + `profile-shell.tsx`). Page header wrapped in `.pa-wrap`; edit-pencil → `.pa-icon-btn` (currentColor). **Hero** keeps the accent gradient card but gains an overhanging `PAClip` + the chunky offset shadow (radius 28→18) so it reads as a clipped photo on the desk; glass stat row unchanged. **Self-contained snapshot cards** went white `.pa-sheet` with `--w-ink*`: mood-signature (+ `mint` washi, `overflow:hidden` dropped so the tape isn't clipped), saved-articles + article-reactions rows (`pa-card-lift`, `--w-tint` icon tiles). **Settings-list cards deliberately kept themed `var(--surface)`** (paper shadow + radius 18, hairline border) — they embed `CustomMoodManager` (shared with the not-yet-reskinned `/profile/settings`) + `PersonalEventsManager`, which use theme `--ink/--surface` tokens and would be invisible on an always-white sheet in dark mode; their rows therefore stay theme-tokened (dark-safe). `PremiumTeaser` re-tinted to a fixed lavender gradient + `--w-ink*` + `--purple-strong` (was `--hero-grad` → would go dark + hide `--ink`). Achievements "→" link → `--purple-strong`; loading skeleton → `--w-tint`. Sign-out / clear-entries / feedback bottom-sheets left themed (functional, dark-safe). Loose header text stays theme `--ink*`. All settings logic/toggles/locale-switch/feedback + `/api/profile*` untouched. |
| 2026-06-05 | Subscription → "Paper Desk" | Reskinned `/profile/subscription` (`subscription-shell.tsx`) — self-contained (only generic `BottomSheet`/`TrialConfirmSheet` modals, left themed), so it went full paper. Shared `CARD` flipped to white `.pa-sheet` → all 3 states' feature cards, the month-usage cards, and the Free plan card are white paper with `--w-ink*` (Free-card progress track `#F2F0F5`→`--w-tint`, `FreeLimitRow`/`FreeLockRow` text → `--w-ink*`, lock icon `#FCA45B`→`#E08A2B`). The **Paid-Pro dark card** became a plum **folder** (`.pa-tab purple` "✨ Pro Yearly/Monthly" + `linear-gradient(135deg,#2C2435,#3D2E50)` sheet + `PAClip`); the **trial-status**, **premium-upsell**, and **trial-activation** gradient cards keep their colored gradients (white text) but gained chunky offset shadows + an overhanging `PAClip`; their white CTA buttons recolored `#A673F1`→`var(--purple-strong)`. Canceling-notice / portal-error / trial-expired banners → tinted `.pa-sheet` (`--w-ink*`); resubscribe → `pa-btn purple`. Back button → `.pa-icon-btn`. Root `.pa-wrap`; loose h1/h2/subtitles stay theme `--ink*`. Cancel `BottomSheet` + `TrialConfirmSheet` left themed (generic modals, dark-safe). All Stripe portal/checkout/trial + `/api/subscription` logic untouched. |
| 2026-06-06 | Admin overview — article engagement metrics | Added two article-engagement numbers to `/admin` per request ("คนอ่าน" + "คนให้ความรู้สึก"): **ยอดอ่านบทความ** = `coalesce(sum(articles.view_count), 0)` (total reads across all articles — `view_count` is a per-article counter, so this is reads not distinct people, labelled accordingly) and **คนให้ความรู้สึก** = `count(*)` of `article_reactions` (one row per user per article = post-read mood reactions). Both folded into `getOverviewStats()` (Promise.all + two new `OverviewStats` fields, `Number()`-cast so they get thousands separators) — no extra round trip, no page.tsx change. Rendered as a labelled "บทความ" sub-section below the 4 KPI cards: two folder-tab `AdminStatCard`s (lav / purple tabs) in a `minmax(200px,1fr)` grid capped at 524px so they don't stretch. Verified on the real DB (views 222, reactions 2). **Also surfaced per-article in the `/admin/articles` list:** the ambiguous "อ่าน" column (which actually showed reading-time) was relabelled "เวลาอ่าน" and two columns added — **ยอดอ่าน** (`view_count`) + **ความรู้สึก** (per-article reaction count). `GET /api/admin/articles` now also returns `reactionCount` by running a grouped `count(*)` over `article_reactions` and merging by `articleId` (avoids the unqualified-correlated-subquery gotcha); `view_count` was already in the `select()`. |
| 2026-06-06 | Admin — re-add Article CMS to sidebar nav | The article admin (list `/admin/articles`, editor `/admin/articles/[id]` + `/new`, categories `/admin/article-categories`) already existed and was Paper-Desk-styled, but had been removed from the sidebar (`features.md`: "Articles/Categories removed from nav") so it was reachable only by URL. Re-surfaced it: new `IconDoc` (document SVG) in `admin-icons.tsx`; a single **บทความ** nav item added to `AdminSidebar` (placed in the content group, before the Mood Packs divider). Refactored the sidebar's hardcoded `i === 4` divider into a per-item `dividerBefore` flag and the `isActive(href)` check into `isActive(item)` honouring an optional `activePrefix` — บทความ uses `"/admin/article"` so it highlights on both `/admin/articles` and `/admin/article-categories` (note: `startsWith("/admin/articles")` would *not* match the categories path, hence the shorter prefix). Categories kept as a sub-page (not its own nav row): the Articles list header gained a "จัดการหมวดหมู่" ghost link beside "+ สร้างบทความ", and the categories page gained a "← บทความ" back link. No new routes/APIs/logic — pure navigation wiring. |
| 2026-06-06 | Admin user lists — avatars, Trial plan, fixed entry count | Three fixes to the admin "recent users" (overview) + full users list. **(1) Entry count always 0 — root-cause bug:** the correlated subquery `(SELECT count(*) FROM mood_entries WHERE user_id = ${users.id})` rendered `${users.id}` *unqualified* as `"id"`, which Postgres bound to `mood_entries.id` (the inner table's own column) → `user_id = entry_id`, never true. Fixed by aliasing the inner table + qualifying the outer column: `… FROM mood_entries me WHERE me.user_id = "users"."id"` (same fix applied to the users-list `lastEntryDate` `max(date)` subquery, which was likewise null → every user wrongly "inactive"). Verified against the real DB (counts now 2 / 806 / 30, last-entry dates populated). **(2) Google avatar not shown:** the overview recent-users table only rendered gradient initials and never selected `users.image`; now selects `image` and renders the photo (signed/Google URL) with an initials fallback, mirroring the users-list pattern. **(3) Plan badge missing Trial:** plan was binary Free/Premium; added a **Trial** state using the canonical `tier.ts` rule — `premium` if `isPremium`, else `trial` if `trialEndsAt` is in the future, else `free` (so a comped/paid premium user who also has trial fields still reads Premium, e.g. the admin). Overview uses `AdminBadge` (`info`/`warning`/`free` = Premium/Trial/Free); the users list got the matching 3-state pill (peach `--w-tint-warning` for Trial). New `UserPlan` type + `plan` field on `RecentUser`; `getRecentUsers` computes it server-side. No schema/endpoint changes. |
| 2026-06-06 | Admin charts — readable labels (`AdminBarChart`) | The DAU + AI-Usage bar charts drew only bars: the per-bar `label` (date) and `maxVal` were computed but never rendered, so admins saw 30 unlabelled bars with no day or count. Rewrote the shared `AdminBarChart` to be self-explanatory while honouring the **≥14px min-font rule** (axis micro-labels at 11-12px would have broken it, so per-bar always-on labels were rejected): SVG now stretches full-width (`preserveAspectRatio="none"`, fixed `viewBox` width 1000) so bars fill the card instead of floating in dead space and an HTML overlay can align to each bar by `(i+0.5)/n` percentage; added a **"today" callout** headline (`วันนี้ N {unit} ↑/↓ % เทียบค่าเฉลี่ย N วัน`, green/red delta vs the period mean), an interactive **hover/tap tooltip** (paper card: Thai `D MMM` date + per-series breakdown via the legend map + total, all 14px), a faint **max gridline + max-value label**, and **first/mid/last date ticks**. New opt-in `unit` prop drives the callout + tooltip noun (`"คน"` for DAU, `"ครั้ง"` for AI Usage) — generic callers without `unit` just get the chart. Latest bar stays full-opacity (= today), others dim to .5 and brighten on hover. `"06-14"`→`"14 มิ.ย."` via a TH month map; non-`MM-DD` labels pass through. Component made interactive (`useState` hover) — still `"use client"`. No data/endpoint changes; `tsc` clean + `next build` passes. |
| 2026-06-06 | Admin → "Admin Desk" (Paper Desk) | Reskinned the whole `/admin` back-office to the paper system, read as a **filing-cabinet desk of case files**. **Foundation:** the shared `A` style object (`admin-ui.ts`) drives nearly everything, so it was flipped first — `card`/`cardFlush` → paper sheets (`--w-surface` bg, folded `4px 18px 18px 18px` radius, chunky warm shadow `0 18px 40px -20px rgba(60,40,20,.45)`, `--w-rule` hairline for dark definition); `th`/`td`/`eyebrow`/`sectionTitle` → `--w-ink*`/`--w-tint`/`--w-rule`; buttons → paper (`btnPrimary` chunky peach offset shadow, `btnInk` chunky `--w-ink`/`--bg`, `btnGhost` paper-outline, `btnDanger` `--w-tint-danger`, `btnSmall` paper pill w/ soft shadow); `input` → `--w-surface` + `--w-rule-strong`. **Shared components:** `AdminStatCard` rebuilt as a **folder-tab card** (`.pa-tab` colour over `.pa-sheet`, new `tab` prop peach/purple/mint/yellow/lav/ink — each page assigns varied tabs so the KPI row isn't monotone); `AdminBadge` variants remapped to the semantic tint tokens (`--w-tint-{success,warning,danger,info}` + `-fg`, `admin`→`--w-ink`/`--bg`, premium keeps the peach→purple brand gradient); `AdminPageHeader` gained a gradient file-label bar + `--w-rule` underline (title stays loose theme `--ink`, dark-safe); `DataTable` zebra/borders/pagination → `--w-*`; `AdminBarChart` inherits the paper `A.card`; `AdminSidebar` kept its dark plum (`--ink`) cabinet rail but the active nav item now reads as a **folder tab pulling out to the right** (`borderRadius:0 13px 13px 0`, peach left bar + peach-tinted icon), logo tile enlarged + glow, "ADMIN" eyebrow recoloured peach. **New token:** `--w-tint-warning`/`-fg` added to `globals.css` (`:root` + `.dark`) for the badge warning variant. **Shells:** stat-card rows in overview/entries/feedback/user-detail given varied `tab` colours; overview recent-users table + plan pill → `--w-*`; `user-detail-shell`/`admin-articles-shell`/`admin-article-categories-shell`/`admin-article-editor-shell` local `CARD` consts flipped to the paper sheet shape; article published pill → `--w-tint-success`; packs create/upload purple buttons given the matching purple chunky shadow. Layout `<main>` content wrapped in `.pa-wrap` (bg stays themed `var(--bg)` — admin inherits the app light/dark theme, so **no** forced-light `.pa-desk` island; dark-safe throughout since `--w-*` alias the themed palette). Note: `--w-ink`/`--w-rule`/`--w-tint`/`--w-surface` are aliases of `--ink`/`--hairline`/`--surface-2`/`--surface`, so the token swaps are render-neutral — the visual shift is entirely the paper shapes, chunky shadows, folder tabs, and refined rail. `w-btn`/`w-input` in article shells left as the shared themed form controls. Dead `stat-card.tsx` (unused) left untouched. All admin server actions / queries / `/api/admin/*` logic untouched; `tsc` clean + `next build` passes. |
| 2026-06-06 | Login → "Paper Desk · Sign-in slip" | Reskinned `/login` (`page.tsx` left brand panel + `login-form.tsx` + `mobile-login-feed.tsx`), the last big pre-paper surface. **Concept:** the login is a reading desk — the existing articles-first "read before you sign up" strategy becomes a stack of **paperclipped clippings** (same treatment as the saved-articles reskin: tilted white `.pa-sheet` + `pa-card-lift` + `PAClip` + framed `ArticleArt`/photo cover + tone category + clamped title/excerpt + date), and the form is a **paper sign-in slip** — a purple folder-tab `.pa-sheet` ("✦ เข้าสู่ระบบ") with a `PAClip`, DM-logo brand header, and restyled primitives: paper inputs (`--w-surface-2` + `--w-rule-strong`), chunky `.pa-btn`-style peach primary (offset shadow, `active:translate-y`), paper-outline Google button, danger-tint (`--w-tint-danger`) error box (was bare red text), email shown as a `--w-surface-2` pill with a purple "ย้อนกลับ", verify-sent buttons → paper (ink resend + outline back). Dropped the decorative `Sparkles` cluster (AI-slop) for the brand logo. **Page-level:** `.auth-form` + `.auth-brand` backgrounds forced to `var(--bg)` (cream desk) so the slip + clippings float as paper; `.auth-split`/`.auth-mobile` get `.pa-wrap`. Dropped the local `TONE_MAP` for shared `paper/tone` helpers; brand panel now passes raw `tone` to `MobileLoginFeed` so its clippings can render `ArticleArt` fallbacks too. Sheet text `--w-ink*` (theme-adaptive → dark-safe, sheets flip with theme), loose desk text theme `--ink*`. All text ≥14px (bumped eyebrows/meta from 11px). **Auth logic 100% untouched** — email-first step machine (landing→email→password/register/google_only/verify_sent), `check-email`/`register`/`signIn`/`resend-verify` calls, `guestEntry` cookie handoff, `trackLogin`/`trackSignUp`. `tsc` clean + `next build` passes. **Known pre-existing quirk (not changed):** the mobile sticky "อีเมล" button does `window.location.href="/login"`, which reloads the same mobile feed (the desktop `LoginForm` is `display:none` on mobile) — flagged for a separate fix, out of scope for a visual reskin. |
| 2026-06-06 | Article reactions → "Paper Desk · Saved clippings" | Reskinned `/profile/article-reactions` (`article-reactions-shell.tsx`) to match its twin (the saved-articles reskin above), sharing the exact clipping layout — tilted white `.pa-sheet` + `.pa-card-lift`, `PAClip`, framed `ArticleArt`/photo cover, tone-tinted category pill, clamped title/excerpt, `PAMark` h1 + count. **Distinguishing signature (the whole point of keeping the two pages separate):** each clipping is **stamped with the mood the reader felt** — a `PASticker` (real mood face via `DEFAULT_MOODS` colour) overhangs the cover's bottom-right corner like a wax seal, and the footer **names** that mood in a mood-tinted chip (`MoodIcon` + label, `${color}2E` bg) beside a muted reading-time + the `--purple-strong` "อ่านต่อ →". Header eyebrow 💭 "ความรู้สึกของคุณ", h1 highlight uses **lavender** `PAMark` (vs saved's peach) so the sibling pages read as a set but not identical. Empty state = `mint`-washi sheet with a floating `PASticker` (happy/mint, `.pa-float`) + `pa-btn purple`. Dropped the local `TONE_MAP`/`bgHue` for the shared `paper/tone` helpers. Data/`GET /api/articles/reactions` + the `ReactedArticle` shape (incl. `moodTypeId`) untouched; all text ≥14px; `tsc` clean. |
| 2026-06-06 | Saved articles → "Paper Desk · Saved clippings" | Reskinned `/profile/saved-articles` (`saved-articles-shell.tsx`), which was still on the old `.card`-row pattern (its twin `/profile/article-reactions` got the matching treatment right after — see the next row). **Concept:** the bookmarked list reads as a pile of paper clippings pinned to the desk. Each row is a white `.pa-sheet` + `.pa-card-lift` with a small **alternating tilt** (`TILT` array, `rotate(±0.4–0.7deg)`) that straightens + raises on hover; a metal `PAClip` overhangs the top-left holding a **framed cover** (96px, `3px solid #fff` + soft shadow) — real `coverImageUrl` photo if present, else the tone-based `ArticleArt` SVG (reused from the articles grid). Body: tone-tinted category pill (`toneBg`/`toneHue`), 2-line title (`--w-ink`) + 2-line excerpt (`--w-ink-2`) clamps, footer = `--w-tint` reading-time chip + a quiet `--purple-strong` "อ่านต่อ →". Header is loose on the desk (theme `--ink*`, dark-safe): 📎 "คลังของคุณ / Your library" eyebrow + `PAMark` peach-highlighted h1 (`บทความที่ <เก็บไว้อ่าน>` / `<Saved> articles`) + subtitle with a live count. Loading → 3 paper skeleton clippings (`--w-tint` blocks, `.pulse`, same tilt); empty → washi-taped (`lav`) `.pa-sheet` with a floating bookmark glyph (lavender radial halo + `.pa-float`) + `pa-btn purple` "ไปอ่านบทความ" (height-override so Thai tone marks aren't clipped, like `EmptyToday`). **Deliberate divergence from the sibling reactions page:** saved-articles carries *no* mood `PASticker` — the sticker is reserved for `/article-reactions` so the two pages stay visually distinguishable. Dropped the local `TONE_MAP` in favour of the shared `paper/tone` helpers. Data/`GET /api/articles/bookmarks` + the `SavedArticle` shape untouched; all text ≥14px (min-font rule); `tsc` clean. |
| 2026-06-05 | Static pages → "Paper Desk" | Reskinned the static/content + onboarding pages. **`/terms` + `/privacy`** (`terms-page.tsx`, `privacy-page.tsx`): hero → clipped gradient folder (`PAClip` + chunky shadow, dropped `.card`/`overflow:hidden`, floating emoji moved to bottom-right), TL;DR → washi-taped (`yellow`/`lav`) white `.pa-sheet` with `--w-ink*` rows + `--w-rule` dividers; the 7 numbered sections stay loose long-form text on the desk (theme `--ink*`, dark-safe); back btn → `.pa-icon-btn`. **`/forbidden`** (`forbidden-page.tsx`): `.pa-wrap` on `<main>`, back → `.pa-icon-btn`, primary CTA → `pa-btn purple`, ghost CTA → `.pa-filter`; the `ForbiddenCharacter` + faint 403 kept. **`/welcome-pro`** (`welcome-pro-shell.tsx`): unlocked-features checklist → white `.pa-sheet` (+ mint washi, `--w-ink*`), gradient icon + chunky shadow, CTA → `pa-btn` (peach). **`/welcome`** new-user onboarding (`welcome-new-user-shell.tsx`): right-column feature cards → white `.pa-sheet` (`--w-ink*`, decorative blob/offset kept, inner texts marked `position:relative` so they sit above the blob), new-member trial banner → fixed lavender-tint `.pa-sheet` (`--w-ink*`), primary CTA → `pa-btn ink` + "Go home" → white paper button; own top-bar/header kept. All loose page text stays theme `--ink*`; all data/trial/`/api/welcome`+`/api/trial/activate` logic untouched. |
