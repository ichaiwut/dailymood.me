# DailyMood.me — Design

## Design Direction
- **Concept:** "DailyMood Web App" — warm cream canvas, Vercel/Linear-style desktop layout, vibrant purple/mint/peach palette
- **Reference:** Claude Design handoff (60 screens, 1280px desktop + 440px mobile artboards)
- **Approach:** Desktop-first responsive; topbar 64px full-bleed, content container max-width 1180px centered; mobile fallback with bottom nav
- **Layout pattern:** Topbar full-bleed + content centered (~1180px) — เหมือน Vercel/Linear
- **AI-slop avoidance:** No generic gradients or glassmorphism; color comes from mood palette, not decoration

## Typography (next/font)
- **UI sans (Latin)**: `Urbanist` (Google, weight 400-800) — all headings, body, buttons
- **UI sans (Thai)**: `Noto Sans Thai` (Google, weight 400-700) — Thai fallback
- **Style:** Bold (800) for headings, 700 for buttons/labels, 500-600 for body

## Color Palette (hex, light)
| Token | Value | Use |
|---|---|---|
| `--bg` | `#FBF6EE` | warm cream canvas |
| `--bg-grad` | `#FBF6EE` | flat warm cream (no gradient) |
| `--surface` | `#FFFFFF` | card backgrounds |
| `--surface-2` | `#F4EEE6` | warm tint tile |
| `--hairline` | `rgba(26,19,32,0.08)` | borders, dividers |
| `--hairline-2` | `rgba(26,19,32,0.12)` | stronger border |
| `--ink` | `#1A1320` | dark purple-black (primary text) |
| `--ink-2` | `#4A3F55` | secondary text |
| `--ink-3` | `#8C8497` | tertiary gray |
| `--primary` / `--purple` | `#A673F1` | primary brand (active nav, labels, avatar) |
| `--accent` / `--peach` | `#FCA45B` | CTA buttons, FAB, accent |
| `--mint` | `#85ECCB` | calm/positive mood |
| `--yellow` | `#FDCB56` | neutral mood |
| `--blue` | `#9ACDE2` | sad mood |
| `--lavender` | `#D4BEE4` | anxious mood |

## Mood Colors (7 moods)
| Mood | Emoji | Color | Score |
|---|---|---|---|
| Happy | 😄 | `#FCA45B` (peach) | 5 |
| Calm | 🙂 | `#85ECCB` (mint) | 4 |
| Neutral | 😐 | `#FDCB56` (yellow) | 3 |
| Sad | 😔 | `#9ACDE2` (blue) | 2 |
| Angry | 😠 | `#FEAD8D` (light peach) | 1 |
| Anxious | 😟 | `#D4BEE4` (lavender) | 2 |
| Tired | 😴 | `#A673F1` (purple) | 2 |

## Layout System
- **Frame:** 1280px (full page width including topbar)
- **Container:** `.w-container` max-width 1180px, margin 0 auto, padding 0 32px
- **Desktop topbar:** 64px height, full-bleed, backdrop-blur, horizontal nav (วันนี้/ปฏิทิน/สถิติ/AI) + "+ บันทึก" CTA + theme toggle (🌙/☀️ 36px square) + language toggle (🌐) + avatar
- **Mobile:** BottomNav (floating pill, peach FAB center), topbar hidden
- **Grid layouts:** `.grid-2col` (2.2fr 1fr), `.grid-sidebar` (320px 1fr), `.grid-stats-4` (4-col), `.home-grid` (1.7fr 1fr)
- **Auth:** `.auth-split` (1.1fr 1fr) — gradient brand left + form right; mobile hides brand panel
- **Admin:** `.admin-layout` (240px sidebar 1fr) — dark sidebar (#1A1320)

## Visual Signatures
- **Rounded cards** — `.card` class, `border-radius: 14px`, white bg with 1px border `rgba(26,19,32,0.08)`
- **Desktop topbar** — frosted glass (backdrop-blur + 85% opacity), DMLogo SVG gradient (peach→pink→purple)
- **Mobile bottom nav** — white pill (`border-radius: 38px`) with purple active state, peach FAB elevated center
- **Streak card** — right sidebar, large number (44px) + 14 streak bars + 🔥 emoji
- **AI weekly summary** — dark card (#1A1320→#2A1F33), golden sparkle icon (#FFC899), white text
- **Smart Log modal** — centered 720px dialog, backdrop blur, 22px radius, heavy shadow
- **Mood picker (hero)** — inline buttons in gradient hero card (lavender→purple), `minWidth: 72px`, `whiteSpace: nowrap`
- **Tag chips** — pill-shaped with white bg + 1px border or mood-colored bg for detected mood
- **Eyebrow labels** — `.w-eyebrow` class, 11px, weight 800, uppercase, letter-spacing .06em, `--ink-3` color
- **Stat cards** — left colored border (4px solid), large value (36px/800), eyebrow label above
- **Icon buttons** — 44×44px with 14px radius, `#F8F6FB` bg
- **Labels** — uppercase 12px `font-weight: 800`, purple color, 0.4px letter-spacing

## Pages / Screens

| Page | Description | Status |
|------|-------------|--------|
| Home | **Desktop:** 2-col grid (1.7fr main + 1fr sidebar). Main: greeting hero (lavender→purple gradient) with inline mood picker, AI composer card, day-axis timeline, 3-col entry grid. Sidebar: AI weekly summary (dark card), streak card (large number + 14 bars), mini calendar. **Mobile:** single column | Done (v2 redesign) |
| Login | **Split-screen:** gradient brand panel left (peach→purple, tagline, mood emoji row) + form right. Mobile: brand panel hidden, form only | Done (v2 redesign) |
| Smart Log | **Centered 720px modal dialog** (not full-screen). Backdrop blur rgba(26,19,32,.55). Header: sparkle icon + "บันทึกด้วย AI" + close X. Content: textarea with **mood-adaptive placeholder** (changes when mood is selected — Free: static prompts per mood, Premium: Gemini-personalized prompt), mic/image buttons, AI suggestion area, action buttons | Done (v2 redesign) |
| Calendar | **2-col grid** (2.2fr calendar + 1fr stats sidebar). Main: segmented toggle, AI summary, patterns, 7-col calendar grid, Ask AI bar. Sidebar: avg mood/streak/logged stat cards (colored left border) + mood legend | Done (v2 redesign) |
| Timeline | Integrated into Calendar tab via segmented toggle (Calendar/Timeline). Reverse-chronological feed grouped by day (TODAY/YESTERDAY/WEEKDAY), mood type filter chips, entry cards (48px mood swatch + title + time + 1-line note + tag emojis). Data: `/api/calendar/timeline`. `/history` redirects to `/calendar` | Done |
| Stats | Functional period toggle (Week/Month/Year, #F4F2F7 pill bg). Average mood line chart (purple #A673F1 gradient fill, adapts to period length) with **AI annotation pins** (Premium: glowing purple circles with pulse animation on anomaly/best/worst points, hover/tap shows dark tooltip with AI explanation + tag refs; Free: 1 blurred ghost pin + "✦ ปลดล็อก AI Annotations — PRO" upgrade CTA) + **Timeline Markers** (vertical dashed lines at holiday/personal event positions — red `#F43F5E` dashed for holidays, blue `#3B82F6` dashed for personal events, 40% opacity, emoji circle above chart at `PY-6`, rendered behind the line path). Delta badge (green ↑ / red ↓ vs previous period). Mood mix donut (center label adapts to period). Highest Mood Day card (full-width mood-colored bg + emoji). Real activity impact from tag-mood correlation — diverging bars (purple=positive, peach=negative), tag emoji heuristic, min 5 entries/tag, cap 6 rows. Free: rows 4-6 blurred with gradient overlay + "See all with Premium" pill. Year toggle disabled for free (dimmed + inline upsell toast). Insights link card (pastel gradient #FAF7FE→#FDE8DA). Bottom nav: Stats tab replaces Insights tab (bar chart icon) | Done |
| AI Insights | Hero gradient summary card (#A673F1→#C89BF5→#FCA45B) with Read Full (inline expand) + Share (Web Share API / clipboard fallback with "Copied!" toast). Pattern cards: tag badges (PATTERN orange #FCA45B, CORRELATION purple #A673F1, ALERT red #F26B6B) + optional sparkline SVG (60×24, purple line). Suggestion card (#FAFFF8 bg, #DEF1D5 border): green "TRY THIS" badge, thumbs up/down + "Add to routine" feedback pills (persisted in D1). Streak card (#FAF7FE bg, fire emoji). Free: hero preview (headline + first sentence only) + locked overlay. Premium: full feed. Cached weekly in D1 (delta-3 invalidation). Loading skeleton, empty state, too-few-entries state, error state. Back to Stats link in header | Done |
| Entry Detail | Mood hero card (full-width, mood-colored bg, giant faded 200px emoji), note section, AI summary gradient card (#F4EBFE→#FDE8DA) with Gemini-generated Thai summary (`**bold**` → `<strong>`; teaser fallback when no summary), tags pills, edit/compare buttons | Done |
| Edit Entry | **Desktop 2-col** (grid-2col 2.2fr 1fr). **Left:** page header (eyebrow "กำลังแก้ไข · บันทึกที่ N" + h1 "แก้ไขบันทึกของ{weekday} · {date}"), horizontal mood picker row (7 moods, selected=mood-colored bg, min-width 72px), 3-field date/time/period row (date input + time input + period dropdown เช้า/บ่าย/เย็น auto-derived from time), note textarea (500 char limit) with toolbar row (Bold B toggle=`**markdown**`, Voice mic, Image photo picker, ✦ AI re-analyze gradient button), error banner, tags section (# pills + inline add + suggested tags with dashed yellow border), image section (replace/delete overlay). **Right sidebar (sticky top:32px):** live preview card (mood-colored bg, mood icon 72px, label, weekday·time, period, tags joined by ·), AI insight card (sparkle icon + summary with **bold**→`<b>` + auto-reanalyze note), keyboard shortcuts card (⌘S save / Esc cancel / ⌘↵ AI — real `keydown` handlers), danger zone card (red 1.5px border #FCA5A5, warning text, × delete button → confirm modal). **Mobile:** sidebar hidden (`edit-sidebar` `!important`), date/time stacks vertically (1fr), toolbar wraps AI re-analyze to its own row, mood picker container has `overflow:hidden` to clip horizontal scroll. Danger zone duplicated below form via `edit-mobile-only` class (hidden on desktop). Bottom bar (Cancel + Save) rendered via `createPortal(document.body)` to escape main element's `z-index:0` stacking context, positioned `bottom:104px` to sit above BottomNav | Done (v2 redesign + mobile refinement) |
| Settings | Custom mood manager (Premium), mood pack picker (grid cards, 2-col, preview 4 icons per pack, selected=primary border + checkmark, PRO badge on premium packs, lock icon + "Upgrade →" for locked packs, instant save on tap with rollback on error) | Done (replaced by Profile → Settings) |
| Profile Overview | "You" tab. Hero card (purple→peach diagonal gradient, white text, 28px radius, avatar 76px initials circle with accent color + edit pencil overlay). Hero stats row (streak/entries/avg in translucent white panel). Mood signature card (eyebrow label, headline, horizontal stacked bar, top 3 caption). Achievements horizontal scroll (colored ring earned, dashed locked). Settings list (6 rows, color-tinted 42px icon tiles, chevron). Footer (help/sign out outline buttons, version). Bottom nav "You" tab → `/profile` | Done |
| Edit Profile | Modal top bar (back + "Edit profile" + Save purple text). Avatar 120px circle + camera FAB (📷 peach circle, triggers file input on click) + upload spinner overlay during upload + "Remove photo" button when custom avatar exists + premium teaser text for free users. Client-side optimize via `optimizeImage()` (WebP, 1600px max), 2MB server limit, R2 storage. Fields: Display name (gray pill input), Email (read-only + verified badge), Bio (textarea ≤160 chars). Delete account outline button at bottom | Done |
| Profile Settings | Grouped sections on tinted bg. Reminders (check-in toggle, time, days). Appearance (theme 3-option segmented: Light/Dark/Auto; mood palette 3-option: Neon/Tempered/Mono with swatch previews). Language (EN/TH radio). Privacy (toggles). Custom moods (Premium). Data (export, clear). About (help, feedback, terms) | Done |
| Achievements | Hero progress (cream→lavender gradient, 88px circular ring, % + "X of Y unlocked"). Filter pills (All/Earned/In progress/Locked with counts, black active/gray inactive). 2-col badge grid (earned: white card, colored 2px border, icon tile, ✓ date; in-progress: lavender card, dashed border, grayscale icon, progress bar + %) | Done |
| Subscription | `/profile/subscription`. Hero card (deep purple gradient #7B4FD3→#A673F1→#C89BF5, 28px radius, glow shadow). Status pill (✨ ACTIVE / ⏳ ENDING SOON). Plan title "DailyMood Pro" + renewal date. 2-col stat panel (NEXT CHARGE + MEMBER FOR) on translucent white. Billing section: NavRow rows (💳 Payment method, 📋 Billing history, 💡 Switch to yearly). All actions → Stripe Customer Portal. Cancel: outline gray button → BottomSheet (😢 emoji, confirm/keep buttons). Canceling state: peach notice band with resubscribe CTA. Free users: centered upgrade card with ✦ icon + "View Pro plans" link to `/pricing` | Done |
| Admin Dashboard | `/admin`. Sidebar layout (240px fixed, `var(--ink)` bg, peach active border). Gradient logo + "ADMIN" badge. SVG icon nav (6 items). Content: `var(--bg)` bg, max-width 1200px. TH-only. 4 KPI stat cards with delta (users, premium, Stripe revenue MTD, AI calls). DAU bar chart (30d, SVG). Recent feedback list. Auth: `ADMIN_EMAILS` env var | Done |
| Admin Users | `/admin/users`. Search + select filter. DataTable with avatar circles (peach→purple gradient), name, email, plan badge (AdminBadge premium/free), entry count, date, delete. Page number pagination | Done |
| Admin Entries | `/admin/entries`. DataTable with AdminBadge for AI source (info/neutral variants), mood emoji+label, user link. userId filter with clear button | Done |
| Admin AI Usage | `/admin/ai`. 4 stat cards (tokens, cost THB, NLP calls, Vision calls) + 2 cache stat cards. AdminBarChart (SVG, purple NLP + peach Vision stacked). Top users leaderboard. Token/cost tracking via `ai_usage.tokens_in/tokens_out/estimated_cost_thb` columns | Done |
| Admin Feedback | `/admin/feedback`. Status filter tabs (ทั้งหมด/รอดู/ตอบแล้ว/เก็บถาวร). Feedback items: type emoji, star rating, status badge (AdminBadge), archive action. Two-panel: user feedback + AI suggestion aggregation. DB: `feedbacks.type/rating/status` columns | Done |
| Admin Mood Packs | `/admin/packs`. 3-col grid cards. Pack card: 7-icon preview row (28px circles), label + tier badge (AdminBadge premium/free), edit/upload/delete buttons. Create form. SVG upload to R2 | Done |
| 404 Not Found | Standalone (no TopBar/BottomNav). Back arrow + "Error 404" header. Blue circle (200px) kawaii character (dot eyes + smile) with peach "?" floating badges. Faded "404" giant text behind (purple 8% opacity). Title + subtitle + body copy. Black pill "Back to home" button (home icon) + ghost "Report broken link" button (links to profile feedback). `fade-in` + `pop` animations | Done |
| 403 Forbidden | `/forbidden`. Standalone. Back arrow + "Error 403" header. Orange circle (200px) kawaii character (closed eyes + blush) with purple lock badge (44px circle, bottom center) + purple sparkle decorations. Faded "403" giant text. Purple uppercase "PRIVATE ENTRY" label. Title + body copy. Purple pill "Sign in to different account" button (lock icon) + ghost "Back to my journal" button | Done |
| Privacy | `/privacy`. Back arrow + "Privacy" title header. Hero card (purple gradient #7B4FD3→#A673F1→#C89BF5, 28px radius, shield 🛡️ floating icon, "YOUR DATA" label, bilingual title, 3 badge pills: ENCRYPTED / NO TRACKERS / NO ADS). "TL;DR" purple label + 4 summary rows in card (icon in colored square + title + body). 7 numbered sections (purple number circles + bold title + body paragraph with **bold** rendering). `fade-in` animation | Done |
| Terms | `/terms`. Back arrow + "Terms" title header. Hero card (peach gradient #E6952E→#FCA45B→#FDCB56, 28px radius, handshake 🤝 floating icon, "THE DEAL" label, bilingual title, effective date text). "THE SHORT VERSION" peach label + 4 summary rows (✅💛💎❤️ icons). 7 numbered sections (peach number circles). Same structure as Privacy | Done |

## Components

| Component | Purpose |
|---|---|
| `BottomSheet` | Generic reusable bottom sheet: scrim (`rgba(10,10,10,0.32)`), panel slides up from bottom (28px top corners), drag handle, body scroll lock, Escape dismiss, `sheet-open`/`sheet-close` CSS animation with deferred unmount |
| `DaySheet` | Calendar day sheet content: date header with prev/next arrows, fetches `/api/log?date=`, renders `EntryMiniCard` list, empty state with "+ Log mood" CTA, loading skeleton |
| `EntryMiniCard` | Compact entry card for sheets: mood color bg (20% alpha) with ghost icon, mood label + time, 2-line note clamp, tag pills, Edit icon + "Open full entry" black pill CTA |
| `AiSummaryCard` | Calendar AI summary card: gradient bg (#FAF7FE→#FDE8DA), ✨ header, Gemini 2-3 sentence summary with **bold** → `<strong>`, 3 highlight chips (Best day/Hardest day/Top trigger), "Tell me more →" CTA. Free tier: 1st sentence + blurred chips + "Unlock with Premium" |
| `PatternsFeed` | AI pattern cards below calendar grid: icon + explanation + "View →". Locked state for free users |
| `AskAiBar` | NL search bar: dashed lavender border, rotating placeholder queries, expands on tap, inline answer with date chips |
| `TopBar` / `TopBarClient` | Purple avatar circle (initials) + day label + greeting with name + search/bell icon buttons |
| `BottomNav` | Floating white pill nav: Home, Calendar, FAB (+), Stats, Profile. Purple active, peach FAB |
| `HomeShell` | Home page: streak strip, AI composer, mood picker row, recent entries |
| `SmartLogModal` | Full modal: mood grid, note textarea (purple border), voice/camera, AI detection with gradient section, tag pills |
| `MoodIcon` | SVG mood icon from R2 pack, falls back to emoji |
| `MoodPicker` | 7-mood grid (legacy, replaced by inline row in HomeShell) |
| `LoginForm` | Email-first Linear-style: email → password / register / google_only / verify_sent |
| `VoiceButton` | Browser STT (Web Speech API), locale-aware (TH/EN) |
| `StatsShell` | Stats page: functional period toggle (Week/Month/Year with Year=premium gate), mood line chart card (SVG, adapts to period length, delta badge), mood mix donut + highest mood day 2-col grid, real activity impact panel (tag-mood correlation, diverging bars, premium blur on rows 4-6), Insights link card. Accepts `tier` prop |
| `InsightsShell` | AI Insights page: hero gradient summary card (Read Full inline expand + Share via Web Share/clipboard), pattern cards (tag badges + optional sparkline), suggestion card (thumbs up/down + "Add to routine" feedback pills → POST `/api/insights/feedback`), streak card. Free: preview + locked. Premium: full feed. Cached weekly. Accepts `tier` prop |
| `TimelineFeed` | Timeline view inside Calendar tab: mood type filter chips, day-grouped entry cards (mood swatch, title from aiSummary/note, time, tag emojis), tap → entry detail. Replaces `HistoryShell` |
| `HistoryShell` | ~~History/Timeline page~~ (deprecated — redirects to Calendar tab) |
| `EntryDetail` | Entry detail page: mood hero card with giant emoji + intensity bar, note section, AI summary gradient card, tag pills, edit/compare buttons |
| `ProfileShell` | Profile overview: hero gradient card (accent-aware gradient, avatar initials, stats row), mood signature card (stacked bar + headline), achievements horizontal scroll, settings list (icon tiles), sign out confirmation sheet. Accepts `tier` prop |
| `ProfileEditShell` | Edit profile form: avatar + accent color picker (6 colors), display name + email (read-only) + bio fields, delete account button. Fetches/patches `/api/profile` |
| `ProfileSettingsShell` | Settings: reminders section (toggle + time + days), appearance (theme + palette segmented pickers), language radio (EN/TH with locale switch), privacy toggles, custom moods (Premium), data (export + clear), about links. Accepts `isPremium` prop |
| `AchievementsShell` | Achievements: hero progress ring (SVG circle, % unlocked), filter pills (All/Earned/In progress/Locked), 2-col badge grid with BadgeCard sub-component (earned/in-progress/locked states). Fetches `/api/profile/achievements` |
| `SubscriptionShell` | Subscription management: hero gradient card (deep purple), status pill, plan title, renewal date, 2-col stats (next charge + member-for). Billing NavRows (payment, history, switch yearly) → Stripe Portal. Cancel BottomSheet. Free: upgrade card. Fetches `GET /api/subscription`, portal via `POST /api/stripe/portal` |
| `AdminSidebar` | Admin sidebar nav (client): 240px fixed left, gradient logo + "ADMIN" badge, SVG icon nav (6 items: Home, Users, Edit, Heart, Sparkle, AI), peach active border, version+email footer |
| `AdminStatCard` | Admin KPI card: eyebrow label (11px uppercase), large value (26px 800), optional delta with color (+green/-red), optional sub text |
| `AdminBarChart` | SVG bar chart: stacked multi-series bars, title + legend, responsive viewBox. Used for DAU and AI usage charts |
| `AdminBadge` | Status/type badge pill: variants (premium/free/admin/success/warning/danger/info/neutral) with preset colors |
| `AdminPageHeader` | Page header: title (24px 800) + optional subtitle + optional action buttons slot |
| `admin-ui.ts` | Shared style constants: card, cardFlush, th, td, buttons (primary/ink/ghost/danger/small), statGrid, filterBar, input, eyebrow. COLORS semantic palette |
| `admin-icons.tsx` | 13 SVG icon components for admin nav + actions (Home, Users, Edit, Heart, Sparkle, Ai, Bolt, Dots, Search, Download, Filter, Check, X) |
| `DataTable` | Admin generic typed table: columns with optional render fn, zebra striping, page number pagination, rowKey, emptyText, pending opacity. Uses `A.th`/`A.td` from admin-ui |
| `OverviewShell` | Admin dashboard: 4 AdminStatCard KPIs (users/premium/revenue/AI) + DAU AdminBarChart (30d) + recent feedback list |
| `UsersShell` | Admin users: search + select filter, DataTable with avatar circles + AdminBadge plan pills + delete. URL-driven pagination |
| `UserDetailShell` | Admin user detail: back link, info card (toggle premium + delete buttons), info grid, stat cards, recent entries table |
| `EntriesShell` | Admin entries: DataTable with AdminBadge AI source, mood emoji+label, user link. userId filter with clear button |
| `AiUsageShell` | Admin AI usage: AdminBarChart (SVG stacked, purple NLP + peach Vision) + top users leaderboard (ranked email links) |
| `FeedbackShell` | Admin feedback: status filter tabs, feedback items (type emoji + rating stars + AdminBadge status + archive action), suggestion aggregation panel |
| `PacksShell` | Admin mood packs: 3-col grid cards with icon preview + AdminBadge tier, create form, edit inline, SVG upload to R2 |
| `NotFoundCharacter` / `ForbiddenCharacter` | Pure CSS+SVG kawaii circle characters for error pages. 404: blue circle with dot eyes + smile + peach "?" badges. 403: orange circle with closed eyes + blush + purple lock badge + sparkles. Both use `pop` animation on badges |
| `NotFoundPage` | 404 page client component: standalone layout (back arrow + header), character + faded "404" text, copy section, home + report buttons. Uses `useTranslations("errors")` |
| `ForbiddenPage` | 403 page client component: standalone layout, character + faded "403" text, "PRIVATE ENTRY" label, copy, sign-in + back buttons. Uses `useTranslations("errors")` |
| `PrivacyPage` | Privacy policy page: hero card (purple gradient + shield emoji + badge pills), TL;DR summary card (4 icon rows), 7 numbered sections with `**bold**` rendering. Uses `useTranslations("privacy")` |
| `TermsPage` | Terms of service page: hero card (peach gradient + handshake emoji + effective date), TL;DR summary card (4 icon rows), 7 numbered sections. Uses `useTranslations("terms")` |
| `PersonalEventsManager` | CRUD manager for user's personal important dates (settings page). Emoji input (48px) + label + month/day selects + purple add button. Event list with emoji tile + label + date. Free: max 3 + premium teaser. Premium: unlimited |
| `SpecialDayBanner` | Inline banner for today's holidays/personal events. Renders colored chips: holiday `#FFF0F3`/`#BE123C`, personal `#EFF6FF`/`#1D4ED8`. Used on Home hero and DaySheet |
| `ShareCard` | Shareable mood card rendered as a real 1200×630 DOM node (Open Graph ratio), captured to PNG client-side. Two templates: `streak` (🔥 + giant number + encouragement line) and `signature` (top mood emoji + label + %, full-width mood-color proportion bar, top-4 legend). Light/Dark theme via `palette()`. Bottom-right watermark (purple sparkle tile + "DailyMood" / "dailymood.me"). **Hardcoded HEX only** (mood colors from `DEFAULT_MOODS`, no CSS vars) so it's theme-independent; **no remote images** (avoids canvas taint). `forwardRef` so the modal can target the unscaled node for capture |
| `ShareCardModal` | Bottom-sheet modal opened from Stats "แชร์" button: template tabs (Streak/Mood mix), Light/Dark segmented toggle, live scaled preview (full-res card scaled via `transform` + `ResizeObserver`), actions Share/Save/Copy. Capture via `html-to-image` `toBlob` (double-pass for Safari font readiness). Share = Web Share API with PNG file (falls back to text+url, then to download); Save = download PNG; Copy = `ClipboardItem` image. Privacy note: "no notes or personal details" |
| `ArticlesShell` | `/articles` overview in the "Paper Desk · Magazine grid" aesthetic. White paper-sheet cards on the normal themed body background (`.pa-wrap` has no surface fill; `color: var(--ink)`). Header: `.pa-chip` eyebrow + themed `<h1>` with `PAMark` peach highlight (highlighted word forced dark for contrast on peach) + `.pa-filter` category pills (ink active) + search toggle. Featured folder (`.pa-tab` + `.pa-sheet`, PAClip, cover, repurposed footer = `PASticker` + date + `.pa-btn.purple`) at 1.35fr; 3-card side stack (washi-taped `132px 1fr` rows, alt rotation) at 1fr; remaining articles in `.pa-rest-grid`. Covers via `<Cover>` (photo else `ArticleArt`). `.pa-card-lift` hover straighten+raise |
| `paper/*` primitives | `src/components/paper/`: `ArticleArt` (6 tone-based inline-SVG covers, `slice` fill, no remote images), `PAClip` (paperclip SVG), `PASticker` (tone→mood disc using real `moodIconUrl` face), `PAMark` (marker-pen highlight), `tone.ts` (`TONE_HUE`/`TONE_BG`/`TONE_MOOD` maps + `asTone`). All `.pa-*` CSS lives in `globals.css` scoped under `.pa-wrap`; `--w-ink*`/`--w-rule*` are scoped dark tokens for text **inside** the always-white sheets (sheets stay light in both themes), while loose text uses themed `--ink` |
| `ArticleDetailShell` | `/articles/[slug]` reader in the "Paper Desk" aesthetic — wrapped in `.pa-wrap` (white paper folders on the normal themed body background, **no** cream surface — same as the list page). Loose on-background text (back link, meta, `<h1>`, subtitle) uses theme-adaptive `--ink*`; text inside the white `.pa-sheet` cards / plum card / pills uses the scoped `--w-ink*`. Top bar = paper-pill save/share buttons (inline heart/share SVGs). Header = meta line + `<h1>` + subtitle (no per-article keyword to `PAMark`, so plain h1). Hero = tone-colored `.pa-tab` (category label) over a `.pa-sheet` with `PAClip` + real cover photo (else `ArticleArt`) + corner `PASticker` (tone→mood). Body via `<ArticleBody variant="paper">`. Outro = `.pa-tab.ink` + plum-gradient `.pa-sheet` (radial glow + white sticker) carrying the AI key-takeaway / guest walls. **New** post-read mood-reaction card (`.pa-sheet` + clip + 5 `PASticker` buttons → existing `/reaction` endpoint + analytics; guests get visual-only feedback). Sidebar (sticky): TOC folder (`.pa-tab.ink`) with a **real** scroll-progress bar (`progress` state), Related folder (`.pa-tab.mint`). Journal-prompt card intentionally omitted (no backend). All text ≥14px |
| `ArticleBody` (`variant`) | Markdown→JSX renderer (prose + numbered tip-cards). `variant="default"` (admin preview) keeps the themed card; `variant="paper"` (detail page) renders each numbered tip as a `.pa-sheet` folder with alternating washi tape + chunky tone-colored number badge. Section `id`s preserved for TOC scroll-spy. `.pa-wrap .article-prose` re-tones prose colors to the paper ink tokens |

## Smart Log Flow (UX)

1. User taps a mood on home row (or AI composer card, or FAB +) → modal opens with mood pre-selected
2. "How was your day?" heading + mood picker grid + note textarea (purple-bordered)
3. **Quick Save** (no text) — saves mood only as manual entry
4. **AI Analyze** (with text) — Gemini returns mood + tags → gradient detection panel with animated tag pills
5. User edits mood, removes/adds tags, then taps **Save** → entry appears in timeline

## Design Decisions Log

| Date | Decision | Reason |
|------|----------|--------|
| 2026-05-02 | Modern + Vivid style | ผู้ใช้เลือก |
| 2026-05-02 | Mobile-first | เผื่อ mobile app ในอนาคต |
| 2026-05-05 | AI confirm flow (suggest → edit → save) | ลด false positives จาก NLP |
| 2026-05-05 | Multi-entry/day timeline | สอดคล้องกับ smart logging |
| 2026-05-05 | Client-side image optimize (WebP, max 1600px) | ลด Worker CPU + bandwidth |
| 2026-05-05 | Signed read URLs for R2 (1h TTL) | Privacy ของรูปผู้ใช้ |
| 2026-05-06 | Default mood icons → custom SVG pack hosted on R2 | Unified brand-curated illustration set |
| 2026-05-07 | **Moodly redesign** — drop Clean Wellness → Moodly style. Urbanist font, purple/mint/peach palette, floating pill nav, streak strip, AI composer card. Based on Moodly Figma reference | ผู้ใช้ซื้อ Moodly Figma และต้องการ redesign ทั้งหมดให้เหมือน reference |
| 2026-05-07 | Mood colors: peach=happy, mint=calm, yellow=neutral, blue=sad, lavender=anxious, purple=tired | จับคู่กับ Moodly palette |
| 2026-05-07 | AI Composer card on home (not modal) with caret animation | Centerpiece UX — AI ควรเด่นชัดบนหน้า Home |
| 2026-05-07 | Implement all 10 design screens from Claude Design handoff | Calendar, Stats, AI Insights, History, Entry Detail — ทั้งหมดจาก Moodly design prototype |
| 2026-05-09 | Calendar Day Sheet (bottom sheet) | Daylio-style bottom sheet on day tap — keeps calendar context vs. full-screen push. Generic `BottomSheet` base extracted for reuse |
| 2026-05-09 | Calendar AI features (Summary + Patterns + Ask AI) | AI turns calendar from passive log to reflective surface. Single Gemini call per month for summary+patterns, cached in D1. Premium gating: free sees 1st sentence + blur + locked states. Pattern rings overlay on grid. Ask AI bar at bottom with NL search |
| 2026-05-09 | Timeline view merged into Calendar tab | Two views of one dataset via segmented control. Reuses stats-shell segmented control pattern (#F4F2F7 track, white pill). Filter chips = mood types (not tags). Entry title = aiSummary ?? first line of note ?? mood label. Separate `/api/calendar/timeline` endpoint (no signed URLs = faster). `/history` redirects |
| 2026-05-09 | Stats page redesign — functional period toggle + real activity impact | Period toggle now fetches different data windows (7/30/365d). Activity impact uses real tag-mood correlation from entries (no Gemini, pure data). "Best Day" → "Highest Mood Day" (copy guardrails). Delta badge computed server-side vs previous period. Year toggle premium-gated. Bottom nav: Stats replaces Insights tab |
| 2026-05-09 | AI Insights redesign — caching + feedback + hero actions | Weekly D1 cache (delta-3 invalidation like calendar AI). Suggestion feedback persisted in `suggestion_feedback` table. Hero card gets Read Full + Share. Pattern cards get mini sparkline SVG. Free tier: preview headline + locked state (data stripped at API level). Stats → Insights via link card |
| 2026-05-10 | Profile tab ("You") — 4 screens | Profile Overview replaces old Settings as bottom nav destination. Hero card with user-selectable accent color (6 options) driving gradient. Mood signature computed from 30-day distribution. Achievements with 12 badges, auto-earned on API check. Settings grouped into Reminders/Appearance/Language/Privacy/Data sections. `/settings` redirects to `/profile/settings` |
| 2026-06-03 | Social Share Cards rendered client-side (`html-to-image`), NOT `next/og`/Satori | Spike proved Satori drops Thai tone marks over upper vowels (หนึ่ง→หนึง, ที่นี่→ทีนี) — unacceptable for brand-quality cards. Browser DOM capture shapes Thai+emoji correctly. Cards use hardcoded HEX (theme-independent) + no remote images (no canvas taint). Free for all tiers (growth tool w/ watermark). MVP = Download/Share/Copy; deferred X auto-unfurl `/s/[shareId]` (needs server headless browser) + Highlight Entry template (needs note-blur) |
| 2026-05-10 | Subscription Management (`/profile/subscription`) | PRD adapted from iOS StoreKit → web Stripe. All billing actions via Stripe Customer Portal (no custom billing UI). Deep purple hero card distinct from profile's accent-based gradient. Subscription state stored in DB via webhook (stripeSubscriptionId, currentPeriodEnd, cancelAtPeriodEnd, planInterval) for fast reads. Cancel confirmation via reusable BottomSheet. Free users see upgrade CTA (never hidden per premium-gating rule). Profile NavRow links premium→subscription, free→pricing |
| 2026-05-10 | Admin Panel (`/admin`) — 5 pages | Separate layout outside `[locale]` — sidebar dashboard style, no TopBar/BottomNav. Auth via `ADMIN_EMAILS` env var (hardcoded email, no DB schema change). Server Components + Server Actions (no admin API routes). TH-only UI. Reuses same CSS variables and Tailwind. Dashboard → Users (CRUD) → Entries (browse/delete) → AI Usage (charts) → Feedback (user + AI suggestion) |
| 2026-05-11 | 404 + 403 error pages — kawaii character style | Standalone pages (no TopBar/BottomNav) matching Moodly Figma reference. Pure CSS+SVG characters (no image assets). 404: blue circle + "?" badges, reassuring copy. 403: orange circle + lock badge + sparkles, private entry context. Root `not-found.tsx` wraps own `NextIntlClientProvider`. 403 at `/forbidden` inside `[locale]` tree. "Report broken link" links to existing profile feedback feature |
| 2026-05-11 | Privacy + Terms legal pages | Human-readable legal pages matching Moodly design. Same structure: gradient hero card (purple for privacy, peach for terms), TL;DR summary with icon rows, numbered detail sections. Content adapted to DailyMood's actual stack (Cloudflare D1/R2, Gemini, Stripe, Resend). No auth required. Linked from Settings About section (NavRow with href) + Pricing footer. Bilingual TH/EN via i18n |
| 2026-05-13 | Edit Entry redesign — 2-col desktop layout | Matching Figma reference with form left + live preview sidebar right. Horizontal mood picker row (not grid toggle). Period selector auto-derived from time input. Bold toolbar uses **markdown** markers (no rich text dep). Real keyboard shortcuts (⌘S/Esc/⌘↵). Sidebar is sticky. Mobile: sidebar hidden, fixed bottom bar. Image upload teaser shown for free users (fixes premium-gating rule violation) |
| 2026-05-16 | Avatar upload on Edit Profile | Premium-only. Camera FAB wired to file input (accept=image/*). Client-side optimize via `optimizeImage()` → POST `/api/profile/avatar`. R2 key `users/{userId}/avatar/{ulid}.webp`, old avatar deleted on re-upload (after DB commit to prevent orphan). `users.imageKey` column stores R2 key; `users.image` kept for Google OAuth URL. Render priority: imageKey (signed URL) > image (Google) > initials. TopBar server component queries DB for imageKey to show fresh avatar everywhere. Remove button resets to Google photo or initials (DELETE endpoint, no premium gate). `router.refresh()` after upload/remove to invalidate server-rendered avatar. Blob URLs revoked via cleanup ref to prevent memory leaks |
| 2026-05-16 | Edit Entry mobile refinement | Bottom bar was invisible on mobile because parent `.fade-in` uses CSS `transform` (animation), which creates a containing block that breaks `position:fixed` for descendants. Fix: render bottom bar + delete modal via `createPortal(document.body)`. Layout's `<main z-index:0>` creates a stacking context that traps `position:fixed` descendants below the BottomNav (z-index:40) regardless of their own z-index — the portal escape solves this too. Position the bottom bar at `bottom:104px` to sit above the BottomNav rather than overlap. Sidebar's inline `display:flex` needed `!important` in CSS to be overridden by `display:none` on mobile. Danger zone (delete entry) duplicated below the form via `edit-mobile-only` class because the sidebar is hidden on mobile — without it, users couldn't delete entries on small screens |
| 2026-05-16 | Site-wide copy rewrite — formal but natural Thai | Pass over `messages/th.json` + `messages/en.json` to remove translated/AI-generated feel without going chatty. Removed `เล่า`/`บอก` asks per CLAUDE.md tone rule, dropped overly casual particles (`นะ`, `ลอง...`, `อีกสักหน่อย`), smoothed choppy bullet-style sentences into natural Thai. English: tightened chatty phrasing to polished but warm. Privacy + Terms: kept all technical content (Cloudflare R2, TLS, signed URLs, Google Gemini, Stripe, Resend, OAuth, AI cache, hotlink, uptime) — only the surrounding language was reformed |
| 2026-05-24 | AI Flashback & Cognitive Reflection | Flashback card on Entry Detail page for negative moods (score ≤ 2). Gemini searches past entries with similar emotions, generates 2-3 sentence reflection with **bold** key phrases referencing how user overcame a similar situation before. Blue gradient card (#F0F7FF→#E8F4FD→#F5F0FF) with clock icon + "ย้อนดูตัวเอง" header, past date reference, AI disclaimer. Free tier: dashed teaser card with 🕰️ emoji + upgrade CTA. No cache — on-demand generation via extended `GET /api/log/[id]`. Triggers only for sad/angry/anxious/tired moods. Premium only |
| 2026-05-22 | AI Chart Annotations | Glowing AI-annotated pins on mood trend SVG chart. Gemini analyzes moodTrend + tags to detect 2-5 anomalies/highlights per period. Pins use SVG `<animate>` for pulse glow effect + `<filter>` for blur. Tooltip rendered as HTML div overlay (not SVG title/foreignObject) to avoid clipping — positioned via viewBox→screen coordinate projection using `getBoundingClientRect()`. Free tier: 1 ghost pin with blur filter + upgrade CTA pill. On-demand generation with DB cache (`chart_annotations_cache`, delta-3 invalidation). All periods (week 7pts, month 30pts, year 12pts). Extends existing `/api/stats` response — no separate endpoint |
| 2026-05-22 | AI Guided Journaling Prompts | SmartLogModal textarea placeholder now changes based on selected mood. Free: static pre-written prompts per mood (7 moods × TH+EN, instant). Premium: Gemini-generated personalized prompt using recent tags/moods as context, cached per user/mood/date in `journal_prompt_cache` table for daily variety without redundant calls. Custom moods (Premium): Gemini adapts using mood label. UX: placeholder updates silently — no spinner, user can type immediately, personalized prompt fades in if Gemini resolves first. AbortController prevents stale prompts on rapid mood switching. Follows CLAUDE.md tone: no "เล่า"/"บอก", gentle/open. API: `GET /api/ai/journal-prompt`, no NLP quota impact |
| 2026-05-18 | Location picker on mood entries | Optional location field via `LocationPicker` component. Pin icon + text input (Google Places Autocomplete) + GPS crosshair button in a single row matching existing field container pattern (`var(--surface)` bg, `var(--hairline)` border, `borderRadius: 14`). Google Maps SDK lazy-loaded via `@googlemaps/js-api-loader` only when component mounts (not in layout.tsx). GPS: browser Geolocation → Google Geocoder reverse lookup → "City, Country" string. Clear button (×) when value present. GPS loading uses existing `ai-spin` CSS animation for the spinner. GPS error shown inline in `#D14343`. Placed below mic/photo toolbar in SmartLogModal, as labeled section in EditEntryShell. Display: pin icon + text in `var(--surface)` pill on EntryDetail, one-line truncated in TimelineFeed EntryCard and DaySheet mini card. All tiers (no premium gate). Graceful degradation: component returns null if `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` is not set |
| 2026-05-24 | Public Holidays + Personal Events | Calendar grid: top-left 7px dot indicator — red `#F43F5E` for holidays, blue `#3B82F6` for personal events. Distinct from existing AI pattern dots (bottom-center). DaySheet: colored chip row above entries — holiday chip: `#FFF0F3` bg + `#BE123C` text, personal chip: `#EFF6FF` bg + `#1D4ED8` text, pill-shaped with emoji prefix. Home hero: `SpecialDayBanner` renders between greeting subtitle and h1, same chip style. Profile Settings: "วันสำคัญ" section after Custom Moods, `PersonalEventsManager` component (emoji input 48px + label input + month/day selects, purple add button, event list with date subtitle). Free: 3 events max with premium teaser card (gradient `#FAF7FE→#FDE8DA`, ✦ PREMIUM badge). Holiday data: Nager.Date API cached in `holiday_cache` table (30-day TTL) |
| 2026-06-02 | Theme toggle on desktop topbar | The light/dark `ThemeToggle` (36px square, `var(--surface)` bg + `var(--hairline)` border, sun/moon SVG) previously only appeared in the mobile topbar — added it to the desktop topbar action row, placed between the "+ บันทึก" CTA and the 🌐 language toggle. Reuses the existing `ThemeToggle` component; `TopBarClient` now calls `useTheme()` and derives `isDark` (same logic as mobile, with `auto` resolved via `matchMedia`). Click toggles light↔dark and persists to `localStorage` |
| 2026-06-02 | SmartLogModal — show selected mood | When a user tapped a mood on the home page, the modal opened without surfacing it, so users were confused about whether their pick registered. Added an always-visible "อารมณ์ของคุณ / Your mood" row at the top of the form: horizontal scroll of mood chips (icon 34px + label stacked, `minWidth 68`), selected chip highlighted (2px `var(--ink)` border + `var(--surface)` bg vs `var(--surface-2)`). Tappable to change the mood before writing. Hidden once an AI suggestion is shown (which has its own mood picker). Also fixed the premium AI journal-prompt generator: it could emit "เล่า/บอก" mid/end-sentence (e.g. "ลองเล่าให้ฟังหน่อยนะ") — the rule now forbids those words in any position and avoids the trailing "นะ" particle (tone rule). Cached prompts regenerate daily |
| 2026-06-02 | Home AI composer — show summary | The inline "AI MOOD ASSISTANT" composer on Home captured & saved the AI summary but never displayed it (only mood pills + tags showed), so the AI's analysis text appeared to vanish after analysis. Added a "สรุป / Summary" card after the tag pills (shown when `composerSuggestion.aiSummary` exists — premium): `var(--surface-2)` bg, purple 14px bold label, `**bold**`→`<b>` rendering — matching the SmartLogModal summary block |
| 2026-06-05 | Article reactions — consumers (admin + user) | Made the post-read mood reaction data actually useful (it was only being stored). **Admin:** article editor (`/admin/articles/[id]`) now shows a reader-reaction breakdown card — per-mood count + % bar (in each mood's colour) + total, aggregated via `GET /api/admin/articles/[id]` → `reactionCounts`. **User:** new `/profile/article-reactions` page (`ArticleReactionsShell` + `GET /api/articles/reactions`) listing articles the user reacted to with the mood they picked, linked from a profile sidebar nav row. Both read the `article_reactions` table; no schema change. Followed existing conventions: admin uses `MoodIcon`/`DEFAULT_MOODS`/local `CARD`; user page clones the saved-articles shell pattern (inline TH/EN, `l()` helper, `.card` rows). |
| 2026-06-04 | Article detail → "Paper Desk · Mood Reader" | Reskinned `/articles/[slug]` to match the paper-desk list page. Ported from design handoff `docs/design_handoff_article_v2_paper/` (the ★ V2 mood-integrated reader artboard). Reuses the existing `paper/*` primitives + `.pa-*` CSS — mostly composition, no new primitives. Decisions vs the mock: the mock's warm cream desk + grain was dropped after review — paper folders sit on the normal themed body background, identical to the list page (loose text uses theme-adaptive `--ink*`, white-sheet text uses scoped `--w-ink*`, so both light/dark work); hero shows the real cover photo (else tone `ArticleArt`) wrapped in the folder, not the generic `HeroArt` sunrise; hero folder tab = category label (no per-article "featured" flag), header h1 left plain (no CMS keyword to highlight); author/byline absent from schema so dropped. **New UI** rendered for the first time: the post-read mood-reaction row (handler/endpoint/analytics already existed but were never in the JSX) and a real reading-progress bar (the `progress` state existed but was never shown). The "save as journal prompt" sidebar card was **skipped** (no backend). `ArticleBody` gained an opt-in `variant="paper"` so the admin preview is untouched. All detail-page text kept ≥14px (min-font rule). No cream desk (`.pa-desk` dropped) — paper folders on the themed bg. **Follow-up:** wired real reaction persistence — new `article_reactions` table (`userId`+`articleId` PK, `moodTypeId`, upsert on re-react; migration `drizzle-pg/0013`), the `/reaction` route now validates the mood against `DEFAULT_MOOD_IDS` and upserts, and `GET /api/articles/[slug]` returns the saved reaction so the sticker stays selected on reload |
| 2026-06-04 | Articles overview → "Paper Desk · Magazine grid" | Reskinned `/articles` to the paper-desk aesthetic (folder tabs, paperclips, washi tape, mood-face stickers, layered/rotated white paper sheets). Ported from design handoff `docs/design_handoff_articles_paper_grid/`. Decisions vs the mock (data the DB lacks): covers use the real photo if present else a tone-based `ArticleArt` SVG; mood-face sticker derived from `articles.tone` via `TONE_MOOD` (peach→amazing, lavender→anxious, mint→happy, yellow→neutral, blue→sad, purple→tired) rendered with the real `moodIconUrl` face; featured footer **repurposed** (no author/role columns) to sticker + date + read-more button; full-page scope (featured + 3 side stack + paper rest-grid) keeping live search + dynamic category pills. `/api/articles` now returns `tone`. **Post-review tweaks:** dropped the warm cream surface/grain — cards now sit on the normal themed body background; removed the floating decorative sticker that overlapped the filter pills; featured read-more button changed from harsh black ink → brand purple (`.pa-btn.purple`). New: `src/components/paper/*` (ArticleArt, PAClip, PASticker, PAMark, tone.ts), `.pa-*` styles in `globals.css`, `titlePre/titleMark/titlePost` i18n keys for the peach-marker heading |
